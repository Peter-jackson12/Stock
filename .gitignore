# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info


# Logs
*.log
test_dash.py


# ==========================================
# Environment & Secrets (비밀 키 및 환경 변수 무시 - 추가됨!)
# ==========================================
.env
*.env
.env.*

# ==========================================
# Practice & Temporary Files (연습용 및 임시 파일 무시)
# ==========================================
practice.ipynb
*.tmp

# ==========================================
# Large Data Files (대용량 데이터 및 DB 무시)
# ==========================================
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# ==========================================
# Run Store (L5) — 실행 결과 산출물
# 매니페스트/parquet 은 재실행으로 복원 가능한 산출물이므로 추적하지 않는다.
# ==========================================
runs/

# ==========================================
# Feature Store (L2) — 배치 피처 산출물
# 원천 데이터에서 재생성 가능하므로 추적하지 않는다.
#   uv run python scripts/build_features.py --date YYYYMMDD
# ==========================================
sampledata/features/

# ==========================================
# 옛 백업 아카이브 (2026-09-15 사용자 보관 zip 에서 복원)
# 2022년 8일 샘플 LOB DB(회귀 기준선 재검증용) + 예전 Daily 매트릭스가 들어있다.
# 수 GB 규모라 git 으로 추적하지 않는다 — 로컬에서만 보존.
# ==========================================
sampledata/old_data/

# 키움 메타데이터 배치 상태/원응답/파일럿 산출물 (운영 Daily와 분리)
sampledata/kiwoom_meta/
sampledata/Daily_kiwoom_pilot/
logs/fchart_probe/
