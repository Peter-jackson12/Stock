# 작업 원칙

`git status --short`로 사용자 변경을 확인하고 [HANDOFF의 현재 인계](HANDOFF.md)를 읽는다.
상세 문서는 [README의 문서 찾기](README.md)에서 필요한 것만 선택한다.

- 요청한 목표 안에서 구현 방법과 순서를 자율적으로 정하고, 독립적으로 가능한 작업은 끝까지 진행한다.
  일상적인 코드·문서 수정과 합성 검증에 재확인을 요구하지 않는다. 막힌 항목은 분리하고 나머지를 진행한다.
- 주 경로는 원본 틱 재생이다. LOB/초봉 변환은 기본 선행 작업이 아니다.
- `sampledata/Daily_baseline`, `sampledata/old_data`, 원본 수집 데이터와 사용자 변경을 보존한다.
- 수집 중에는 가벼운 코드·문서 작업, 작은 합성 테스트와 짧은 관측만 한다.
  수집기 재시작/교체, 추가 OCX 로그인, 운영 DB 대량 처리·전체 백테스트·부하 측정은 장외 검증으로 남긴다.
- 문서의 완료 표시나 PID·로그·파일 존재만으로 정상 종료·저장 완료·데이터 정확성을 확정하지 않는다.
  코드와 관련 테스트로 확인하고, 미확인 값은 미확인으로 둔다.
- 관련된 작은 테스트로 변경을 검증한다. 합성 검증과 운영 적용·실데이터 검증을 구분해 보고한다.
- 완료 시 HANDOFF의 현재 상태·다음 작업을 갱신하고 커밋한다. 검증된 변경은 원격 상태를 확인한 뒤 직접 푸시한다.
  상세 계약은 담당 문서 한 곳에 두고, 새 문서는 README에 연결한다.
- 별도 요청이 없으면 사용자에게 한국어로 답한다. 사람이 함께 읽는 공용 문서는 한국어로 유지한다.

## Codex와 Sonnet 5의 작업 인계

- 사용자가 다른 AI의 작업 결과나 인계를 전달하면 Codex는 현재 저장소와 관련 실행 근거를
  확인하고, 다음 작업을 Sonnet 5에게 지시할 복사 가능한 프롬프트를 대화에 제공한다.
  사용자가 매번 프롬프트 작성을 따로 요청할 필요는 없다.
- 프롬프트에는 작업 목적·범위, 확인할 파일, 완료된 결과와 근거, 구체적인 다음 작업,
  금지 사항, 검증 방법, 산출물, 종료 조건을 포함한다. 작업 규모에 맞게 간결하게 작성한다.
- 확인된 사실·이전 에이전트의 보고·추론을 구분한다. 문서나 통과 건수만으로 완료를 확정하지
  않으며, 필요한 대조만 수행해 완료된 작업을 불필요하게 반복하지 않는다.
- 필요한 자료가 없으면 정확한 확보 대상과 방법을 명시하고, 자료 확보와 독립적으로 가능한
  구현·검증도 지시한다. 운영 관측·로그인 등이 필요한 단계는 오프라인 작업과 구분한다.
- 사용자가 Codex에게 구현·분석을 직접 요청하면 해당 요청을 우선한다. 다른 AI의 보고를
  전달받았다는 이유만으로 Codex가 후속 구현 전체를 자동으로 대신하지 않는다.
- 검증된 변경의 커밋·푸시·fast-forward 병합은 대상 브랜치와 원격 상태를 확인한 뒤 별도
  재확인 없이 처리한다. 사용자 변경을 보존하고 강제 푸시·강제 초기화는 하지 않는다.
  fast-forward가 불가능하면 임의로 이력을 재작성하지 않고 차이와 남은 조치를 알린다.
- 인계 프롬프트는 복사 가능한 코드 블록으로 제공한다. 요청 없이 별도 인계 파일을 계속
  생성하지 않는다. 기존 HANDOFF.md는 완료 작업·현재 상태·다음 작업을 갱신하는 용도로 유지한다.
