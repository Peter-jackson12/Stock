# 작업 원칙

[HANDOFF의 현재 인계](HANDOFF.md)를 읽고 원격 master/열린 PR/최신 CI를 다시 확인한다.
로컬 작업이 승인된 경우에만 `git status --short`로 사용자 변경을 확인한다.
GitHub-only 작업에서는 로컬 status도 실행하지 않으며, 실행한 것처럼 말하지 않는다.
상세 문서는 [README의 문서 찾기](README.md)에서 필요한 것만 선택한다.

- 요청한 목표 안에서 구현 방법과 순서를 자율적으로 정하고, 독립적으로 가능한 작업은 끝까지 진행한다.
  일상적인 코드·문서 수정과 합성 검증에 재확인을 요구하지 않는다. 막힌 항목은 분리하고 나머지를 진행한다.
- 주 경로는 원본 틱 재생이다. LOB/초봉 변환은 기본 선행 작업이 아니다.
- `sampledata/Daily_baseline`, `sampledata/old_data`, 원본 수집 데이터와 사용자 변경을 보존한다.
- 수집 시작/종료 시각·장전/정규장/애프터 구간·KRX/NXT 범위를 판단하기 전에는
  [수집 의사결정 계약](docs/COLLECTION_RUNBOOK.md#collection-decision)을 반드시 읽고,
  거기서 지정한 시장 구간·실제 감시 경로·구독 계획·현재 CLI를 대조한다.
  일반 시장 지식이나 과거 대화로 대신하지 않는다. 침묵 판정 범위를 수집/보존 범위로 해석하지 않는다.
- 수집 중 변경과 관측은 [live 작업 경계](docs/COLLECTION_RUNBOOK.md#collection-live-boundary)를 따른다.
  GitHub branch/PR와 GitHub-hosted 합성 CI는 가능하지만 master 변경·병합은 보류한다.
  운영 체크아웃의 코드/환경 변경·git 동기화·pytest·추가 OCX 로그인은 하지 않는다.
  GitHub-only 범위에서는 가벼운 로컬 관측·raw/evidence·operations_state 조회도 허용되지 않는다.
- 문서의 완료 표시나 PID·로그·파일 존재만으로 정상 종료·저장 완료·데이터 정확성을 확정하지 않는다.
  코드와 관련 테스트로 확인하고, 미확인 값은 미확인으로 둔다.
- 관련된 작은 테스트로 변경을 검증한다. 합성 검증과 운영 적용·실데이터 검증을 구분해 보고한다.
- 완료 시 HANDOFF의 현재 상태·다음 작업을 갱신하고 커밋한다. 검증된 변경은 원격 상태를 확인한 뒤 정상 푸시한다.
  상세 계약은 담당 문서 한 곳에 두고, 새 문서는 README에 연결한다.
- 별도 요청이 없으면 사용자에게 한국어로 답한다. 사람이 함께 읽는 공용 문서는 한국어로 유지한다.
- 커밋 제목과 본문은 한국어로 작성한다. 코드 식별자·파일명·명령어·고유명사는 원문을 유지할 수 있다.
  이 저장소에서 작업하는 모든 에이전트에 동일하게 적용한다.

## 로컬 worktree / 임시 작업 폴더 규칙

- Stock의 별도 checkout/worktree를 `C:\Projects` 최상위에 `Stock-*` 이름으로 흩어 만들지 않는다.
- 기본 worktree 루트는 **`C:\Projects\TotalStock\worktrees\<작업명>`** 으로 통일한다.
  예: `C:\Projects\TotalStock\worktrees\independent-actual-01`.
- main checkout인 `C:\Projects\TotalStock\Stock` 내부에 다른 Git worktree를 중첩 생성하지 않는다.
  main working tree의 untracked/ignore 경계와 작업 삭제를 복잡하게 만들 수 있기 때문이다.
- frozen snapshot, qualification result, replay result 같은 대용량·비추적 데이터는 worktree와 분리한다.
  **기존 `C:\Projects\TotalStock\StockSnapshots\...` evidence는 절대경로 provenance를 깨지 않도록 제자리 보존**하고,
  새로 만드는 데이터는 기본적으로 **`C:\Projects\TotalStock\_data\snapshots\...`** 아래에 둔다.
  소스 worktree와 실제 데이터 보존 위치를 섞지 않는다.
- 임시 worktree 이름은 목적 중심으로 짧게 만들고, 작업 종료 후 제거 가능한지 보고한다.
  다른 활성 작업이 연결된 worktree를 임의 삭제하거나 이동하지 않는다.
- 이미 `C:\Projects\Stock-*` 형태로 만들어진 기존 폴더는 새 작업에 재사용하지 않는다.
  정리 전에는 각 폴더의 Git worktree 등록 여부·dirty 상태·미추적 결과·활성 프로세스를 확인한다.

## 일반 ChatGPT와 로컬 에이전트의 역할

- 일반 ChatGPT 대화는 GitHub 원격 구조·코드·문서·CI·PR 검토/수정의 기본 컨트롤타워다.
  작은 단일 수정까지 습관적으로 넘기지 않는다.
- 대규모 구현·다파일 통합·고위험 감사는 개발용 로컬 에이전트에 의미 있는 묶음으로 맡길 수 있다.
  개발 위임은 개발 전용 checkout/worktree와 합성 검증 범위이며, 운영 PC 관측·배포·동기화·로그인·
  실데이터 처리 승인이 아니다. Windows·OCX·`.venv32`·로컬 프로세스·Git 제외 raw/evidence가 필요한
  운영 단계는 이와 별도로 사용자가 정한 로컬 경로와 승인 범위를 따른다.
- 위임 전 목적·입력·금지 사항·검증·종료 조건이 포함된 완성형 프롬프트를 준다.
  로컬 인계의 모델·추론 설정 권고는 복사할 프롬프트 밖에 둔다. 프롬프트·문서는 로그인 승인이 아니다.
- 원격 사실, 로컬 에이전트의 과거 보고, 이번에 직접 관측한 사실을 구분한다.
  원격만 보고 로컬 working tree가 clean이거나 수집기가 없다고 주장하지 않는다.

## 문서 유지와 읽기 범위

- HANDOFF는 현재 목표·차단 조건·다음 행동·근거 위치만 유지한다. 지난 인계를 계속 아래에 붙이지 않는다.
  대략 12 KiB 이내를 유지하되 중요한 미해결 조건을 삭제해 맞추지 않는다. 필요하면 원문을 보존하고 연결한다.
- BACKTEST_TODO는 현재 체크 항목, RUNBOOK은 실행 계약, PIPELINE_MAP은 코드 연결/진입점 차이를 담당한다.
  상세 정책·긴 로그·과거 원인 분석을 여러 파일에 복제하지 않는다.
- archive는 기본 읽기 대상이 아니다. 과거 사건/판정의 원문이 필요할 때만 날짜·session_id·revision으로 찾는다.
  원문 blob 보존본은 고치지 않는다. 상대 링크는 [보존본 안내](docs/archive/README.md)의 당시 경로에서 연다.
- 문서 정리 후 `tests/test_documentation.py`로 활성 링크·필수 의존 연결·절 링크·보존본·HANDOFF 크기를 확인한다.
  이 검사는 문서의 사실 정확성이나 실제 데이터 품질을 인증하지 않는다.

## 에이전트 사이의 작업 인계

- 사용자가 다른 AI의 작업 결과나 인계를 전달하면 현재 저장소와 관련 실행 근거를 먼저 대조한다.
  후속 작업을 다른 에이전트에 맡겨야 할 때 복사 가능한 프롬프트를 자동으로 제공한다.
  로컬 에이전트 작업 분담이 명시돼 있으면 그 역할을 따르되, 위 개발 위임 범위를 운영 승인으로 넓히지 않는다.
- 프롬프트에는 작업 목적·범위, 확인할 파일, 완료된 결과와 근거, 구체적인 다음 작업,
  금지 사항, 검증 방법, 산출물, 종료 조건을 포함한다. 작업 규모에 맞게 간결하게 작성한다.
- 확인된 사실·이전 에이전트의 보고·추론을 구분한다. 문서나 통과 건수만으로 완료를 확정하지
  않으며, 필요한 대조만 수행해 완료된 작업을 불필요하게 반복하지 않는다.
- 필요한 자료가 없으면 정확한 확보 대상과 방법을 명시하고, 자료 확보와 독립적으로 가능한
  구현·검증도 진행한다. 운영 관측·로그인 등이 필요한 단계는 오프라인 작업과 구분한다.
- 사용자가 구현·분석을 직접 요청하면 해당 요청을 우선한다. 인계를 전달받았다는 이유만으로 역할을 바꾸지 않는다.
- 검증된 변경의 커밋·푸시·병합은 대상 브랜치와 최신 원격 상태를 확인한 뒤 처리한다.
  live 중에는 위 병합 보류 규칙이 우선한다. 사용자 변경을 보존하며, fast-forward 불가 자체는 이력 재작성 사유가 아니다.
- 이력 재작성·강제 푸시는 사용자가 대상 브랜치와 변경 범위를 명시적으로 승인한 경우에만 한다.
  먼저 원본 이력을 복구 가능한 참조로 백업하고, 변경 전후 코드 트리·작성자·부모 관계를 검증한다.
  원격을 다시 확인한 뒤 예상 원격 커밋을 지정한 --force-with-lease를 사용한다.
  원격이 예상과 다르면 중단하고 재대조한다. 보호 규칙을 우회하거나 일반 --force를 사용하지 않는다.
- 강제 초기화(reset --hard 등)는 이력 재작성 승인과 별개다. 사용자가 버릴 변경과 대상을 명시적으로
  승인하고 해당 변경을 복구 가능하게 백업한 경우에만 한다. 단순 동기화·충돌 해결 편의로 쓰지 않는다.
- 인계 프롬프트는 복사 가능한 코드 블록으로 제공한다. 요청 없이 별도 인계 파일을 계속 생성하지 않는다.
