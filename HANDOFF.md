# 현재 작업 방향 — 2026-09-16 사용자 확정

## 새 컨텍스트 시작점

- 최신 후속: `scripts/run_tick_research.py` CLI와 [실행 안내서](TICK_RESEARCH_RUNBOOK.md).
  경로·비용·매수/매도/취소 지연·호가 나이를 명시해야 한다. 오늘 운영 DB 자동 선택은 없다.
  raw v2 메타데이터/원천 시각 표현/깊이 배열/비유한 JSON 값을 엄격하게 검사한다.
  결과에 호가 검증 사유별 횟수와 처리 이벤트 수를 추가했고, 빈 파일과 선택 종목 없음도 구분한다.
  CLI → 합성 파일 → 재생 → JSON 저장 통합 포함 38개 통과(1.67초).
  운영 DB 스캔, 수집기 재시작, 실전 TR/주문 실행은 하지 않았다.
- 푸시는 사용자 직접 수행으로 변경됐다. `git push`를 다시 시도하지 않는다.
  8dbb6ad 커밋까지의 푸시는 자동 승인 검토에 차단되어 에이전트가 완료하지 못했다.
  사용자가 이후 직접 푸시했는지는 원격을 확인하기 전 추정하지 않는다.

- 최신 추가 구현: `collector/raw_v2.py`의 오프라인 SQLite 계약 프로토타입과
  `engine/tick_research_run.py::run_raw_v2()` 파일 연결. 새 파일 전용, raw 필드/UTC 수신
  시각/원천 시각 표현/정규화 이벤트 보존, 연속 공통 순번, 명시적 종료,
  읽기 전용 스냅샷과 전체 스트림 체크섬 검증을 구현했다. 저장 설정은 WAL/FULL이다.
  실제 콜백/큐에 연결하지 않았고 운영 수집기는 그대로다. resume는 새 세션/파일로 해야 한다.
  합성 검증: raw v2 + 결과 저장 테스트 19개 통과(1.26초).
  다음은 실제 키움 콜백 필드/정규화/세션 오류 기록 계약 검토이며 운영 적용·성능 측정은 장외다.
  저장 계약 프로토타입의 성공을 실제 공급자 데이터 검증이나 운영 배포로 취급하지 않는다.

- 사용자 협업 방식: Codex와 Claude를 오가며 교차 검증하고 작업을 이어간다.
  Codex 사용량 소진 시 Claude로 전환할 수 있다. 어느 에이전트든 HANDOFF의 완료 판정을
  그대로 신뢰하지 말고 코드·테스트 근거로 재확인한다. 확인된 사실, 설계/가정,
  합성 테스트 통과, 실제 데이터 검증, 미완료 항목을 구분해 갱신한다.
  미커밋 변경과 실행 중 수집기 상태도 함께 인계하며, 문서만으로 배포 완료를 추정하지 않는다.

- 사용자는 가벼운 작업을 자율적으로 이어가도록 요청했다. 장중에는 수집기 재시작,
  운영 raw 변경/대량 조회, 전체 백테스트를 하지 않는다. Daily_baseline과 old_data 보존.
- 최신 작업: `engine/tick_research_run.py`의 메모리 이벤트 연구 실행/JSON 저장.
  `run_research()`는 수수료 명시를 요구하고, 주문·체결·신호·잔여 보유·취소 이력을 기록한다.
  UUID별 경로로 이전 결과를 보존하고 파일 교체 전에 flush/fsync한다.
  failed / empty input / no fills / open position / flat을 구분한다. 실패는 진단 저장 후 예외로 알린다.
  정규화 이벤트 SHA-256과 관련 코드 파일 SHA-256, 설정을 기록한다. raw 원본 검증이나
  전체 환경 재현성 인증은 아니다. 강제 중단 시 running 상태가 남을 수 있다.
- 최신 검증: `tests/test_tick_research_run.py` 8개 통과(0.97초).
  직전 재생·호가·체결·NXT 합성 검증 82개 통과. 전체 운영 데이터 테스트가 아니다.
  pytest 임시 폴더 샌드박스 권한 오류는 승인된 외부 테스트 실행으로 검증했다.
- raw v2 이벤트 저장 계약/닫힌 파일 리더와 연구 실행기 연결은 위 최신 작업으로 추가했다.
  현재 raw v1을 가짜 공통 순번으로 바꾸지 않는다.
  실제 수집 어댑터 적용·재시작·성능 측정·실제 하루 재생은 장외로 분리한다.
- 수집 상태의 마지막 직접 관측은 08:35 체결 rowid 794→798, 호가 0이다.
  이 기록을 현재 상태로 간주하지 말고 필요하면 로그 tail/마지막 행만 짧게 다시 확인한다.
- 사용자가 코드·문서·테스트의 커밋과 origin 푸시를 요청했다. 다음 에이전트는
  `git status`와 `git log`로 실제 커밋 상태부터 확인하고 기존 변경을 되돌리지 않는다.
  Git 게시와 실행 중 수집기 배포는 별개다. 데이터/로그/사용자 보존 자료는 커밋 대상이 아니다.

## 방향과 구현 이력

**원본 틱 → 이벤트 순서/품질 확인 → 틱 재생 → NXT 전략 → 가상 주문/체결 → 결과**가 주 경로다.
상세 기준: [ARCHITECTURE_TICK.md](ARCHITECTURE_TICK.md).

- 첫 목표는 기존 NXT 돌파 전략의 틱 재현 정확성 확인이다.
- LOB/초봉 변환·초봉용 fs_v1 빌드는 새 틱 백테스트의 선행 단계가 아니다.
- Daily_baseline과 사용자 old_data, 초봉 엔진은 회귀용으로 보존한다.
  5건/-1.301%는 초봉 기준선이며 틱 성과 목표가 아니다.
- 동일 초의 미래 호가 혼입을 합성 DB로 재현하고 `engine/tick_ordering.py`에
  수신 순서 기반 최소 재생 모듈을 추가했다. 순서·호가 부재/노후화·종목/venue 분리·
  미래 이벤트 변경 및 청크 경계 불변성을 작은 오프라인 테스트로 확인했다.
  기존 `NextradeTickEngine`에는 아직 연결하지 않았으므로 기존 엔진의 결함은 남아 있다.
  검증: `tests/test_tick_receive_order.py` 19개 통과(0.91초), `git diff --check` 통과.
- raw v1 연구용 읽기 모듈 `engine/raw_v1_reader.py`를 추가했다.
  명시적 opt-in, 이전 초 호가만 연결, 모든 행/출처 보존, 무호가·stale 표시,
  시각 역행/형식 오류 거부를 구현했다. 수집 종료 인증 기능은 없으므로 호출자가
  종료·확정된 파일만 제공해야 한다. 운영 DB에는 실행하지 않았다.
  검증: 리더 및 수신 순서 모듈 테스트 총 34개 통과(1.23초), 작은 합성 자료만 사용.
- `execution/quote_validation.py`에 두 재생 모듈의 호가 검증 어댑터를 추가했다.
  유한한 양수 가격, 양쪽 양의 정수 잔량, bid < ask를 요구하고 missing/stale을 거부한다.
  동일/역전 호가는 별도 정책 검증까지 보류하며, 최우선 호가 검사만으로 깊은 호가의
  유동성이나 실제 체결을 보장하지 않는다. raw v1의 순서 불확실성은 그대로 유지한다.
  `OrderedTick`에는 선택적 bid_size/ask_size를 추가했으며 결측 잔량을 채우지 않는다.
  관련 합성 테스트 56개 통과(1.24초). 최초 샌드박스 실행은 임시 폴더 권한 오류였고,
  승인된 샌드박스 외부 실행으로 검증했다. 수집기와 운영 데이터는 변경하지 않았다.
- 가벼운 실행 모델 연결 구현: `execution/tick_simulator.py`, `engine/tick_session.py`,
  `strategies/nxt_breakout/tick_research.py`.
  매수·매도 지연, 호가별 잔량 소비/FIFO 부분체결, 취소 지연, 취소 우선 동시각 규칙,
  현금/보유량 한도·수수료, 마감 잔여 주문 만료를 구현했다. 마감에 보유분을 허위 청산하지 않는다.
  새로운 호가 순번마다 잔량을 다시 제공하는 연구 가정이며 시장 충격/실제 큐 위치는 모델링하지 않는다.
  NXT 세 청산 규칙은 각각 독립 전략/계좌로 연결했다. 매크로 필터 활성화는 미연결 오류로 거부한다.
  기존 엔진의 개장 전 진입이 미래 정규장 시가에 의존하는 문제를 새 어댑터에서는
  정규장 시가를 관측한 후 진입하도록 변경했다. 기존 엔진과 거래 동치성을 주장하지 않는다.
  검증: 관련 합성 테스트 82개 통과(1.24초). 임시 폴더 권한 문제로 승인된 외부 실행 사용.
- 현재 연결은 정규화된 메모리 이벤트 기반 연구용이다. raw v1을 가짜 공통 순번으로 바꿔
  넣지 않는다. 메모리 연구 실행/결과 저장은 위 최신 작업으로 추가했다.
  raw v2 오프라인 기록/리더 연결은 위 최신 작업으로 추가했다.
  운영 수집 연결과 실제 데이터 정합성·성과 검증은 남았다.
  장외에서 source/session/순서/벽시각/방향/호가 깊이 계약을 검증한 뒤 운영 연결한다.
- 08:33:00~03 짧은 읽기 전용 관측에서 체결 마지막 rowid 577→587,
  호가 마지막 rowid 0이었다. 체결 적재 진행은 확인했지만 호가 수신은 미확인이다.
  수집 프로세스가 살아 있고 체결이 늘어난다는 사실만으로 전체 수집 정상 판정을 하지 않는다.
- 아래 후속 작업 3~10에서 수행한 초봉 경로 점검은 과거 진행 기록이다.
  그 안의 LOB 변환/초봉 피처/병렬 초봉 엔진 개선 후보를 다음 우선 작업으로 자동 선택하지 않는다.
- 장중은 가벼운 문서·코드 점검과 작은 합성 테스트만 한다. 수집기를 재시작하지 않았고,
  이번 재생 모듈 작업은 운영 수집 코드와 원본 DB를 변경하지 않았다.

---

# 인수인계 — rev.2 코드 재검토 (2026-09-16)

## 판정

검토 대상은 `7a182c0`, 대조 기준은 사용자가 제공한
「2-a 개정 지시서 (rev.2 · 2026-09-15)」 원문이다.
이전의 “§1·§2′·§6·§5 전부 완료” 판정을 철회한다.
§1의 백업 경로 고정과 §2′의 상수 폴백 제거는 코드로 확인했지만,
D-4 날짜 귀속과 D-5 재실행 보존에는 결함이 있었고 D-6은 완료되지 않았다.

## 이번에 확인·수정한 결함

### [P1] D-6: 실제가라는 근거가 없고, 실측은 오히려 분할조정 가격

분할이 없는 표본은 수정주가/실제가를 구분할 수 없다. rev.2 §3의
“시뮬레이션이라는 판정 철회”는 “실제가임을 입증”한다는 뜻이 아니다.

2026-09-16 셸에서 삼성전자 fchart를 직접 읽은 결과:

- 요청: `https://fchart.stock.naver.com/sise.nhn?symbol=005930&timeframe=day&count=4000&requestType=0`
- 응답 chartdata count: `3000` (요청 4000을 보장하지 않음).
- `20180427|53380|53639|52440|53000|606216`
- `20180504|53000|53900|51800|51900|39565391`
- 삼성전자 공식 공시는 50:1 분할과 2018-05-04 신주 상장을 명시한다:
  https://www.samsung.com/global/ir/reports-disclosures/public-disclosure-view.71265/
- 위 응답은 분할 전 가격까지 5만원대로 조정된 가격임을 보여준다.
  따라서 fchart를 절대 가격 판단용 실제가 소스로 승인할 수 없다.

수정: `daily_collector.py`는 OHLCV 파생 파일을 `Daily/unverified_fchart/`에
격리한다. 기존 `Daily/open.csv` 등 5개 파일은 덮어쓰지 않는다.
매니페스트에 `price_basis=split_adjusted_observed`,
`d6_status=blocked_pending_actual_price_source`를 기록한다.
`tradamt`도 실제 거래대금이 아니라 `종가×거래량` 추정치임을 명시한다.
전략 YAML에는 절대 수준=그 시점 실제가, 수정주가=비율 계산 전용 정책을 추가했다.
이 YAML 표기는 소스 검증이나 엔진의 강제 검증 구현을 대신하지 않는다.

**남은 범위:** 실제가 소스 연결·검증, 소비 엔진에서 가격 기준 검사 및
분할 경계 테스트. 기존 Daily 파일의 가격 기준도 자동 인증되지 않는다.
D-6과 전종목 전환은 아직 완료가 아니다. 격리 이후 기존 일봉 입력은
새로 갱신되지 않으므로 실제 운영 배포 전에 이 후속 작업이 필요하다.

### [P1] D-4: 현재 shares가 과거 요청 종료일에 기록됨

기존 `last_date = trading_dates[-1]`는 요청 기간으로 필터링한 결과였다.
오늘 2022년 구간을 요청하면 오늘 shares가 2022년 마지막 거래일에 저장됐다.
휴장일·오래된 캘린더도 같은 소급 기록을 만들었다.

수정: 한국 시간 관측일을 별도로 기록한다. 요청 범위 안의 관측일에만
스냅샷을 만들며 과거 요청일로 소급하지 않는다. 수집 도중 자정이 넘어
관측일이 달라지면 현재 배치에 값을 넣지 않는다. 시가총액은 같은 날짜
종가가 있을 때만 계산하고, 이전 종가를 끌어오지 않는다.
캔들이 없어도 당일 대상 종목은 NaN 행과 사유를 남겨 축적할 수 있다.

### [P1] D-5: 날짜 컬럼 부재와 재실행 데이터 유실

기존 파일은 `code,name,mkt,shares,float`만 저장했다. 날짜는 읽을 때만
파일명으로 생성했다. 같은 날짜 재실행은 파일 전체를 덮어써 다른 종목과
기존 유효값을 지웠다.

수정: 실제 CSV에 `date` 컬럼을 저장하고 날짜·종목 중복을 검증한다.
날짜별로 최초 유효값을 유지하며 결측과 새 종목만 보충한다.
shares가 달라진 재응답의 mkt를 기존 shares에 섞지 않는다.
임시 파일 작성 후 원자적으로 교체해 쓰기 실패 시 기존 스냅샷을 보존한다.
파생 wide 파일은 누적 스냅샷의 종목명과 전부 NaN인 종목도 유지한다.

이전 형식은 읽기 호환만 제공한다. 파일명으로 과거 관측의 진위를 입증할
수 없으므로 기존 잘못 귀속된 값의 자동 재날짜화·삭제는 하지 않는다.
단일 writer 기준이며 동시 수집 프로세스 간 잠금은 후속 작업이다.

## 독립적으로 진행한 작업 및 검증

- §1: `Daily_baseline` / 사용자 제공 `old_data` 보존. 실수집 실행 없이
  테스트는 임시 경로, 회귀는 기존 고정 입력을 사용한다.
- §2′/D-4/D-5: 위 수정 및 오프라인 실패·재실행 테스트 추가.
- §6: 9/15 로그 508개 상태줄(07:03:33~15:34:50) 재생 결과
  경보 0건, 최대 큐 203,692건(09:16:55), 침묵 구간 0건.
  이는 해당 로그의 실측 결과이며 모든 운영 상황의 무오탐 보장은 아니다.
- 사용자 후속 지시: 특정 과거 틱 날짜 복원은 불필요. 기본 회귀에서
  고정 `20260911` 의존성을 제거했다. `--date YYYYMMDD --code 종목`을
  명시했을 때만 틱을 검증하며, 지정한 DB가 없으면 실패로 보고한다.
  날짜 생략은 “틱 미검증”으로 명시하고 틱 통과라고 주장하지 않는다.
- 최종 관련 테스트: `tests/test_daily_snapshot.py`,
  `tests/test_kiwoom_session_monitor.py`, `tests/test_macro_policy.py`,
  `tests/test_verify_phase_a_cli.py` — **53 passed**.
  실행: `.venv/Scripts/python.exe -m pytest <위 네 파일> -q -p no:cacheprovider`
  (이 환경에서는 임시 디렉터리 권한 문제로 권한 확장 실행 사용.)
- 실제 1초봉 회귀: **10/10 통과**, 두 번 모두 **거래 5건 / -1.301%**,
  `run_id=b4fac584`, 거래 목록 완전 일치. CSV/parquet 진입·청산가,
  사유·MAE/MFE, 기존 results와 건수·손익 합계 일치 확인.
  로그: `rev2-regression.log` (로컬, git 미추적).
- 장시간 회귀 실행은 사용자의 9/11 의존성 제거 요청 전에 시작했으므로
  그 프로세스의 최종 요약은 10/11, 종료 코드 1(9/11 DB 부재)이다.
  이를 전체 통과라고 보고하지 않는다. 이후 수정한 기본 CLI는 틱 날짜를
  요구하지 않으며, 그 분기와 명시한 틱 날짜의 실패 처리는 별도 테스트로
  검증했다. 실제 틱 저장·재현성 검증은 앞으로 확보한 데이터로 수행한다.

## 후속 작업 1 — 수집 중간 실패에 대한 보존 (2026-09-16)

- 캔들과 현재 메타데이터 요청을 분리했다. 캔들 통신·XML/숫자 파싱 실패도
  shares 조회를 막지 않는다. 캘린더 조회에는 메타데이터 HTTP 요청을 하지 않는다.
- 네트워크 오류·408·429·5xx는 총 3회까지 0.5초/1초 간격으로 재시도한다.
  그 밖의 HTTP 오류는 즉시 결측 사유를 남긴다. 이는 전종목 부하 검증이나
  키움 TR 호출 제한 대응을 대신하지 않는다.
- 당일 관측은 종목마다 즉시 스냅샷에 저장한다. 다음 종목 처리나 격리된
  가격 파일 저장이 실패해도 이미 저장된 관측은 남는다.
- `Daily/collection_runs/<관측일>_<실행ID>.jsonl`에 시작·종목 관측·완료를
  추기하고 flush/fsync한다. 기존 실행의 사유를 재실행으로 덮어쓰지 않는다.
  started 이후 completed가 없는 로그는 미완료 실행이다(no_data는 데이터 없음).
- 중단된 실행의 파생 wide는 최신 스냅샷보다 뒤처질 수 있다. 재실행 시 누적
  스냅샷에서 재생성한다. 재요청은 수행하며 최초 유효값은 보존한다.
  완료 종목의 TR 요청 생략, 동시 writer 잠금, 전종목 저장 성능은 아직 미구현이다.
- 검증: `test_daily_snapshot.py`, `test_daily_collection_failures.py`,
  `test_verify_phase_a_cli.py` **22 passed**. 캔들 실패·HTTP 재시도·중간 중단·
  재실행·가격 내보내기 실패를 오프라인으로 확인했다.
  엔진과 기준선 입력은 변경하지 않아 장시간 1초봉 회귀를 반복하지 않았다.
- 실수집·배포는 수행하지 않았으며 `Daily_baseline`과 `old_data`는 보존했다.

## 후속 작업 2 — opt10001 유통 필드 확인 및 정규화 (2026-09-16)

사용자가 KOA Studio 조회에 성공해 삼성전자 응답을 제공했다.
`상장주식=5846279`, `시가총액=14528002`, `현재가=248500`,
`유통주식=4405778`, `유통비율=75.4`가 실제로 반환됐다.
로컬 `C:/OpenAPI/data/opt10001.enc`의 OUTPUT 정의에서도 네 필드를 확인했다.
**“opt10001에는 유통주식/유통비율이 없을 것”이라는 예상은 철회한다.**

- 주식수 천 주, 시총 억원 해석 시 계산 시총 14,528,003.315억원과 응답
  14,528,002가 표시 정밀도 범위에서 일치한다. 유통주식/상장주식도
  75.3603788%로 응답 75.4%와 일치한다.
- 이는 사용자 표본의 정합성 확인이다. 응답과 로컬 TR 정의에 단위 설명은
  없어 공식 단위 인증이나 유통비율 산정 정의/갱신 시점 확정으로 취급하지 않는다.
- `collector/kiwoom/stock_meta.py`: 단위를 명시적으로 받는 Qt 비의존 정규화,
  원응답·결측 사유·정합성 진단, 한국 수신일 기준 tidy 저장 연결을 추가했다.
  shares는 주 단위로 환산하지만 원천이 천 주 정밀도이면 그 정밀도가 유지된다.
- 시총과 주식수×현재가가 맞지 않으면 shares/mkt를 결측 처리한다.
  유통비율 범위·유통주식 비율 불일치도 결측 처리한다. 빈 값에 상수를 넣지 않는다.
  가격은 정합성 검사에만 쓰며 장전 OHLC 0을 일봉으로 내보내지 않는다.
- 수신 시각에는 시간대를 요구하며 snapshot date는 한국 수신일에서 만든다.
  원응답 JSON을 `kiwoom_observations/`에 보존한 뒤 기존 스냅샷 저장소에 반영한다.
- 제공 응답은 테스트 표본으로만 사용했다. 실제 수신 시각/서버가 확인되지 않은
  복사본을 운영 Daily에 넣지 않았다. 실전 로거와 자동 배치 연결도 아직 하지 않았다.
- `test_kiwoom_stock_meta.py` 및 스냅샷/수집실패 테스트: **31 passed**.
- 다음: 단위와 유통비율의 공급자 정의/갱신 시점 확인, 제한된 종목의 실제 TR
  수신·저장 확인, 호출 제한과 재개를 갖춘 배치 연결. D-6 일봉 소스는 별도다.

## 확인이 필요한 후속 단계 (각각 별개 게이트)

1. **§2**: 실제 실행 환경의 fchart 50종목 지연·실패율 측정 후
   `core/universe.py` 연결과 스로틀·재시도·부분 실패 처리 및 벌크 소스 선택.
   이번 삼성전자 단일 호출 성공은 50종목 부하 실측을 대신하지 않는다.
2. **§3-a / 2-b**: opt10001의 네 필드 존재는 사용자 실측과 로컬 TR 정의로 확인.
   단위·유통비율 정의/갱신 시점 및 실제 TR 연결을 검증한 뒤 재개 가능한 배치를
   연결한다. 네이버 경로는 여전히 float 소스가 없고, 키움 자동 수집은 미연결이다.
3. **§3-b만**: 일반 PowerShell에서 pykrx 분할 전후 응답 확인.
   실패 시 공공데이터포털/공식 KRX API의 필드·키·가격 기준 확인 후 백필.
   이 결과 때문에 §1·§2′·§5·§6이나 당일 NaN 축적을 중단하지 않는다.
4. **D-6**: 공식 실제가 소스 및 분할 경계 실측을 확정한 뒤 격리 해제.
   네이버 테스트의 최초 ProxyError는 에이전트 셸 접근 문제였고,
   권한 확장 후 성공했다. 이는 KRX나 pykrx 접근성의 증거가 아니다.
5. **앞으로의 틱 검증**: 실제 축적된 하루의 DB로 `--date YYYYMMDD`와
   체결이 있는 `--code`를 지정해 저장·재현성을 검증한다. 9/11 복원은
   필수가 아니며 당일 수집·스냅샷 축적의 게이트가 아니다.

배포 전 변경 검토와 커밋을 수행한다. 이번 작업은 커밋·푸시·실전 배포를
수행하지 않았다.

## 환경 주의사항

- 체크아웃 2개: 학원(`C:\Projects\03_Personal_Quant\Stock`), 집(`C:\Projects\Stock`). **GitHub로만 동기화됨** — Cowork 세션은 한 번에 컴퓨터 1대만 연결되고, 컴퓨터를 바꾸면 이전 연결은 사라짐(추가되는 게 아니라 대체됨).
- 컴퓨터 옮기기 전 항상: 현재 컴퓨터에서 git commit + push → 도착 후 git pull.
- 폴더 연결은 세션마다 새로 필요할 수 있음(데스크톱 앱에서 폴더 재연결).
- 실데이터(raw_ticks/*.db, temp/*.db, features/*.parquet)는 .gitignore 대상이라 git으로 안 따라감 — 집컴에만 존재.
- `sampledata/old_data/`(2022 샘플 LOB DB + 옛 Daily 매트릭스, 5GB)도 이제
  `.gitignore` 대상 — 삭제하지 말 것, §1 회귀 검증의 유일한 LOB 출처다.

## 출발 전 짧은 점검 (2026-09-16 아침)

- `.venv32/Scripts/python.exe`: 32비트 확인, PyQt5 QApplication/QAxWidget import 성공.
- 주요 변경 Python 파일 구문 검사 및 `git diff --check` 통과.
- 틱 수집기 `kiwoom_universe_logger.py`와 `session_monitor.py`는 HEAD 대비 변경 없음.
- AC/DC 자동 절전 대기값 모두 0(자동 절전 없음), C: 여유 공간 약 1TB 확인.
- 실제 로그인/OCX 생성/수신은 실행하지 않았다. 프로세스 목록 조회는 권한 거부로
  중복 실행 여부를 확인하지 못했다. 사용자 버튼으로 기존 수집기를 시작한다.
- 시작 후 로그인 성공 → 종목 수가 표시되는 실시간 수신 등록 완료를 확인한다.
  구독 완료 메시지만으로 데이터 수신을 입증할 수는 없으며, 거래가 발생하는
  시간에 체결/호가 카운터 증가와 당일 로그를 확인한다.
- 키움 메타데이터 배치는 아직 실시간 수집기에 연결하지 않았다.
  이번 출발 전 점검으로 실수집/자동 로그인/배포/커밋을 수행하지 않았다.

## 후속 작업 3 — 수집 중 읽기 전용 적재 점검

- `scripts/check_tick_collection.py` 추가. 기본 한국 날짜 raw DB를 3초 간격으로
  두 번 열어 체결/호가의 마지막 rowid와 PC 수신 기록 시각을 비교한다.
- 사용: `.venv/Scripts/python.exe scripts/check_tick_collection.py`
  과거 파일은 `--date YYYYMMDD`, 직접 경로는 `--db 경로`, 구조화 출력은 `--json`.
- SQLite `mode=ro`, `query_only=ON`. 누락 DB를 생성하지 않으며 COUNT·전체 스캔·
  체크포인트·수집기 로그인/구독 변경을 하지 않는다. 관측 사이 연결을 닫는다.
- rowid 증가는 정확한 건수가 아니다. unchanged는 관측 구간에서 증가가 없었다는
  뜻이며 장전/비거래 시간 장애 판정이 아니다. progressed도 무누락/저지연 증거가 아니다.
  마지막 시각은 거래소 타임스탬프가 아니라 현재 수집기가 기록한 PC 수신 시각이다.
- 종료 코드 0은 조회 성공, 2는 파일/스키마/접근 등으로 확인 불가를 뜻한다.
  주문 허가나 자동 정상 판정에 사용하지 않는다.
- 테스트 8개 통과: 읽기 전용 강제, WAL 커밋 가시성, 중간 적재 증가, 빈 DB,
  누락 DB 비생성, rowid 간격/리셋, 잘못된 스키마, 대기시간 상한.
- 어제 `20260915_raw.db` 약 7.4GB에서 읽기 전용 확인은 약 0.188초.
  체결/호가 마지막 기록은 15:32:56. 이 조회만으로 수집 종료 사유나 결손을 판정하지 않는다.
- 실행 중 틱 수집기와 보존 기준선은 변경하지 않았다.

## 후속 작업 4 — 재개 가능한 opt10001 파일럿 배치

- `scripts/kiwoom_metadata.py prepare`가 `core.universe` 선언을 읽어 오늘의
  서버·종목·주식수 배수를 고정한 계획 파일을 만든다. 기본 blue_chips 3종목,
  파일럿 상한 50종목. 네이버 50종목 실측이나 전종목 전환을 완료했다는 뜻은 아니다.
- `collector/kiwoom/run_meta_batch.py`는 32비트 Qt/OCX 전용 실행기다.
  로그인한 서버 구분이 계획과 일치해야 opt10001을 요청한다.
  서버 구분이 알 수 없는 값이면 보수적으로 중단하며 실서버로 추측하지 않는다.
- `meta_batch.py` 상태 머신이 원응답/진단과 완료 상태를 별도 SQLite에 저장한다.
  WAL + synchronous=FULL, 송신 전 요청 기록, 상태 DB 전체의 마지막 요청부터
  최소 4초 간격, 동일 실행당 종목별 최대 2회, 20초 타임아웃을 적용한다.
- 같은 날짜/계획 재실행은 완료 종목을 생략한다. 실패·결측·요청 중 중단된
  종목은 재시도한다. 요청 거부(예: -200)나 연결 종료는 배치를 중단한다.
- 요청 ID로 지연 응답을 구분하고, 응답 종목 불일치/자정 경계는 저장 실패로
  기록해 과거 날짜로 정상값을 넣지 않는다. 단일 배치 OS 잠금도 추가했다.
  다른 프로그램의 API 호출량/접속은 이 잠금과 호출 간격의 관리 범위 밖이다.
- 64비트 `import` 단계는 원응답을 다시 검증하고 서버별
  `Daily_kiwoom_pilot/mock` 또는 `live`에 스냅샷을 만든다.
  운영 Daily에 자동 반영하지 않으며, 실시간 틱 수집기는 그대로다.
- 검증: 배치/정규화/스냅샷/유니버스 관련 **55 passed**.
  실제 32비트 Python에서 CLI 도움말 실행 및 OCX 생성 성공,
  KOA_Functions/CommRqData 노출 확인(로그인·TR 전송 없음).
- 준비한 계획: `sampledata/kiwoom_meta/jobs/20260916_ab7168b658a9d019a361.json`
  (mock, blue_chips 3종목, shares_multiplier=1000). 다른 날은 계획을 다시 만든다.
- **아직 미검증:** 실제 로그인 후 서버 구분 반환값, 실제 3종목 TR 이벤트·원응답
  저장·파일럿 반영. 단위와 유통비율 정의/갱신 시점 확인도 별도다.
  오프라인 테스트와 OCX 로딩만으로 실수집 성공이라고 판정하지 않는다.
- 실행법은 README의 “키움 메타데이터 파일럿 배치” 절 참조.
  틱 수집 시작 직전에는 로그인/접속을 변경할 필요 없이 이 준비 상태로 둘 수 있다.
- `Daily_baseline`, 사용자 `old_data`, 실시간 수집기/경보 코드는 변경하지 않았다.

## 후속 작업 5 — 실행기 이벤트 통합 검증 및 전체 테스트

- 실제 Qt/키움 연결을 대체한 이벤트 테스트로 로그인 → 서버 확인 → 요청 →
  TR 수신 → 상태 DB → 파일럿 반영 → 재실행 완료 종목 생략까지 검증했다.
  서버 불일치/알 수 없는 서버, 로그인 오류, 접속 끊김, 응답 읽기 오류,
  창 종료도 검증했다. 실제 키움 TR 송수신 검증을 대신하지 않는다.
- 재현·수정한 결함:
  1. 서버 구분 조회 예외가 이벤트 핸들러 밖으로 빠지던 문제 → 실패 종료 처리.
  2. Qt 창이 닫혀 반환한 0을 배치 성공으로 보고하던 문제 → 배치 complete도 확인.
  3. 타이머 검사 직전에 도착한 제한시간 초과 응답을 받아들이던 문제 → 수신 시각 검사.
- 전체 `tests/` 실행: **349 passed in 15.73s**.
  로그 `integration-tests.log` (로컬 미추적). 새 테스트는 실제 로그인하지 않는다.
  보존 입력을 사용하는 장시간 verify_phase_a 실데이터 회귀는 이번에 반복하지 않았다.
- 사용자 요청으로 시작한 틱 수집기는 PID 22016으로 유지되며,
  07:19:51 로그 체결/호가/대기큐 0 확인. 수집기 코드·접속·구독은 변경하지 않았다.
- 남은 외부 확인은 메타데이터 3종목의 실제 TR 수신 및 파일럿 반영이다.

## 후속 작업 6 — 틱 수집 종료 시 잔여 배치 저장

- 기존 코드는 종료 보고 직후 `is_running=False`로 DB 루프를 끊고 daemon
  스레드를 기다리지 않아 큐/미커밋 배치를 잃을 수 있었다. 저장 건수도 커밋
  전에 올라갔다. `tick_writer.py`로 저장 루프를 분리해 수정했다.
- 종료 순서: Qt 스레드에서 신규 수신 차단 및 구독 해제 → 통계 스레드 종료 →
  수신 종료 시각 확정 → 잔여 큐/배치 전부 커밋 → DB 스레드 종료 확인 → 요약.
  종료 중에는 배치 사이 0.5초 대기를 생략하며, 저장 대기는 수신 결손에 넣지 않는다.
- Ctrl+C와 15:35 통계 루프는 종료를 요청하고 Qt 타이머가 실행한다.
  Qt 종료 이벤트도 같은 경로를 사용한다. DB 건수는 커밋 성공 후 증가한다.
- DB 예외는 롤백하고 큐에서 꺼낸 미커밋 배치까지 잔여 건수에 포함한다.
  오류/잔여가 있으면 정상 종료 문구를 출력하지 않고 종료 코드 2를 반환한다.
  실패 데이터를 재시작으로 자동 복구하는 기능은 없다.
- 정상 종료 시 저장 보강이며 강제 종료/전원 장애 복구 보장은 아니다.
  기존 `synchronous=OFF`는 유지했다. 두 번째 Ctrl+C는 기존대로 강제 종료다.
- 새 오프라인 테스트 11개: 다중 배치, 기존 DB append, 커밋 진행 중 종료,
  커밋/INSERT/DB 열기 실패, 빈 큐, Ctrl+C/Qt 종료/장 마감 배선, 저장 대기 시각.
  전체 **360 passed in 8.39s**, `git diff --check` 통과.
- **실행 중인 PID 22016에는 적용되지 않았다.** 재시작/추가 로그인 없이 유지했고
  07:32:56 로그 체결/호가/큐 0 확인. 이 프로세스는 기존 종료 동작을 사용한다.
  변경분 커밋 후 다음 실행에 적용하고, 실제 종료 로그의 미커밋 0건을 확인할 것.
- `Daily_baseline`, 사용자 `old_data`, 운영 raw DB를 수정하지 않았다.

## 후속 작업 7 — D-6 일봉 소비 경계의 출처 검사

- `DataLoader`가 CSV 경로만 받으면 fchart 격리 파일도 절대 가격 입력으로 읽던
  경로를 확인했다. `core/price_policy.py`에서 로드 전에 알려진 수정주가를 차단한다.
- 새 수집분은 `unverified_fchart/_price_manifest.json`을 CSV보다 먼저 atomic
  replace로 기록한다. 표식은 해당 디렉터리 파일만 설명한다. root의 기존
  `_meta_manifest.json`은 root 가격 파일의 출처 선언으로 해석하지 않는다.
- `BackTestEngine(require_actual_prices=True)` / `engine.main --require-actual-prices`:
  실제가 선언 없음/unknown/수정주가/잘못된 표식이면 실패. 검증 범위 밖 가격 파일도
  거부한다. 옵션이 없어도 알려진 수정주가와 손상된 표식은 실패한다.
- 기본 모드는 기존 출처 없는 기준선의 재현을 유지한다. 이를 `unknown_legacy`로
  콘솔/런 notes에 기록하며 실제가 인증으로 승격하지 않는다. 레거시 파라미터 hash는
  유지하고, 엄격 모드의 정책/출처는 파라미터와 run_id에 포함한다.
- 관련 테스트 41개 및 전체 **372 passed in 8.53s**. 표본에 분할이 없어도
  수정주가 출처면 거부하는 경우, CSV 읽기 전 차단, 로컬 파일 범위, 손상 표식,
  엄격 모드 배선/별도 run_id, CSV 출력 실패 전 표식 저장을 확인했다.
  `git diff --check`, 실제 CLI 도움말 확인 완료. 장시간 실데이터 회귀는 재실행하지
  않았으며 마지막 확인값은 거래 5건 / -1.301%다. 계산식/보존 입력은 수정하지 않았다.
- **D-6 전체 완료 아님:** 실제가 소스 연결, 공급자 데이터 검증, 분할일 기준가 및
  전일 종가 결측 폴백 검증이 남아 있다. 표식은 원천 데이터의 독립 인증이 아니다.
  nxt YAML은 여전히 정책 문서이고, 이번 실행 게이트는 1초봉 엔진에 연결했다.
- 현재 수집 PID 22016 유지, 07:38:59 로그 체결/호가/큐 0 확인. 추가 키움 접속,
  운영 수집 실행, 보존 데이터 변경, 커밋/배포는 하지 않았다.

## 후속 작업 8 — 엄격 모드 전일 종가 및 날짜 경계

- 가격 출처 검사만 켠 상태에서도 전일 종가 결측이면 당일 시가 × 1.3으로
  근사하던 경로를 확인했다. 엄격 모드에서 `strict_previous_close()`를 사용해
  입력 오류를 `PriceBasisError`로 발생시키고 해당 근사를 차단했다.
- 일봉 Name/Code 구조, 실제 달력 날짜 형식, 중복/역순, 양쪽의 당일 행,
  open/close 바로 앞 날짜 일치를 검사한다. 바로 앞 종가가 결측이어도 더 오래된
  가격으로 건너뛰지 않는다. 전일 종가와 LOB 첫 시가는 양의 유한값이어야 한다.
- 가격 오류를 기존 종목별 broad except가 삼키지 않도록 재발생시킨다.
  실행은 성공 CSV/런 저장 전에 실패한다. 하루 DB 연결은 `closing()`으로
  감싸 오류 경로에서도 닫는다. 이미 있던 이전 실행 결과 파일은 삭제하지 않는다.
- 엄격 모드 파라미터에 `reference_input_policy=strict_daily_predecessor_v1`을
  기록해 이전 엄격 모드와 run_id를 구분한다. 레거시 정책/해시는 그대로다.
- 새 테스트 23개 및 전체 **395 passed in 8.78s**. 결측/무한대/0/음수,
  날짜 경계/중복/역순, 주말, DataFrame 비기본 인덱스, 더 오래된 값으로 대체 금지,
  실패 전파·성공 저장 방지·DB 닫기 및 레거시 경로 보존을 검증했다.
  실데이터 장시간 회귀는 반복하지 않았다(마지막 확인 5건 / -1.301%).
- 여전히 미완료: 두 파일에서 함께 누락된 거래일 감지(공식 캘린더 필요),
  분할/권리변동일 기준가, 실제가 공급자 연결. 이 검사로 해당 정확성을 인증하지 않는다.
- PID 22016 및 로그인 유지, 07:48:03 로그 체결/호가/큐 0 확인.
  `Daily_baseline`, 사용자 `old_data`, 운영 raw DB는 변경하지 않았다.

## 후속 작업 9 — fchart 50종목 실측 도구 및 에이전트 셸 측정

- `scripts/probe_fchart.py` 추가. 기본은 HTTP 없는 계획 출력이며 `--execute`로
  측정한다. `core.universe` 규칙/명시 선언을 재사용하고 1~50종목 상한을 강제한다.
  생산 수집 대상은 변경하지 않는다. fchart 파서를 수집기와 공유하도록 분리했다.
- 종목 간 1초, 10초 timeout, 일시 오류 최대 3회 시도. 403/429 또는 3종목 연속
  실패 시 중단한다. 요청 시작/결과를 각각 JSONL로 flush/fsync하고 요약 JSON을 남긴다.
  중단·실패·재시도는 별도 집계한다. HTTP/파싱 성공은 데이터 최신성 인증이 아니다.
- 전체 **405 passed in 8.78s**. 재시도 가시성, 차단 중단, 연속 실패, HTTP 200의
  불량/빈 응답, Ctrl+C 부분 결과, 이전 보고서 덮어쓰기 금지 등을 검증했다.
- 2026-09-16 07:54~07:55 에이전트 셸 실측:
  로컬 규칙 해석 2,221종목 중 seed=0 표본 50개, HTTP/파싱 **50/50 성공**,
  재시도/실패 0회, 총 56.169초. 요청 지연 중앙값 70.765ms, p95 112.24ms.
  첫 10건 중앙값 75.1635ms, 마지막 10건 66.886ms. 지연은 해당 표본의 관측값이다.
- **최신성 문제 확인:** 299910 응답 마지막 날짜 20250213, 101060은 20220117.
  나머지 48개는 20260915. 거래정지/상장폐지 등 원인을 이 응답만으로 판정하지 않는다.
  로컬 key.csv의 최신성 및 현재 수집 유니버스와의 차이를 먼저 확인해야 한다.
- 원기록: `logs/fchart_probe/20260916_075437_227f0424/attempts.jsonl`,
  `summary.json`. 진단 파일은 .gitignore 대상이며 OHLCV/스냅샷을 쓰지 않는다.
- **rev.2 확인 3 완료로 승격하지 않음:** 일반 PowerShell 실측은 별도다.
  environment 표기와 proxy 환경변수 유무는 실제 egress/IP의 입증이 아니다.
  50회 성공은 3,750종목 운영 부하의 보장도 아니다. 전종목 전환/실제가 소스 확정은 보류.
- PID 22016 접속 유지, 07:55:05 로그 체결/호가/큐 0. 추가 키움 로그인이나
  수집 재시작 없이 진행했고 Daily_baseline/old_data/운영 raw DB를 변경하지 않았다.

## 후속 작업 10 — 수집 중 가벼운 파이프라인 연결 점검

- 사용자 요청에 따라 코드/설정/최근 로그만 읽었다. DB 스캔, 외부 요청, 변환,
  피처 생성, 백테스트, 전체 테스트는 실행하지 않았다. README 실행 안내를 정정했다.
- 현재 키움 실행 파일에는 raw 저장 이후 LOB/일봉/피처/백테스트 호출이 없다.
  `run_daily_daemon.py`는 별도 KIS 수집기이며 동일 날짜 raw DB 경로를 사용한다.
  이를 키움 후처리 도구로 실행하면 안 된다. KIS 경로에도 피처/백테스트 연결은 없다.
- 1초봉 엔진 기본값은 feature_source=store이므로 LOB 생성 뒤 피처 빌드가 필요하다.
  틱 엔진은 raw를 직접 읽어 LOB/피처 빌드와 별개다. dashboard는 runs 조회 계층을 쓴다.
- LOB 변환은 원본 DB에 인덱스를 만들고 결과 종목 테이블을 DROP/CREATE/INSERT한다.
  `--limit` 적용 전 인덱스를 생성하므로 소수 종목 제한만으로 가벼운 작업이 되지 않는다.
- README의 20시 수집 표현(코드는 15:35), 8대 일봉 최신화 표현(현재 가격 격리),
  빠진 피처 빌드 단계, 틱 엔진 날짜 생략 예시를 수정했다.
- 후속 코드 수정 후보(이번에는 미수정):
  1. build_lob_db.main은 원본 DB 부재로 result=None이어도 종료 코드 0 반환.
  2. nxt_tick_engine CLI 기본 날짜가 여전히 20260911. 사용자가 실제 날짜를 명시해야 함.
  3. run_parallel은 --require-actual-prices 전달 경로가 없으며, 병합 대상도 이번 실행
     산출물에 한정하지 않고 전략 접두사의 기존 CSV까지 모음.
  4. 현재 키움 경로의 종료 확인 → 후처리 실행을 잇는 별도 운영 절차/실행기는 미연결.
- 07:59:07 하트비트 및 PID 22016 생존 확인. 수신 0건 상태이므로 실제 수신 성공을
  새로 입증한 것은 아니다. Daily_baseline/old_data/운영 DB/실행 프로세스는 보존했다.
