# 현재 인계 — 2026-09-18 / 정규장 저장 종료 대조 완료

## 15:40 예약 실행 결과 — 저장 종료 확인, 품질 인증과 구분

- 세션 `21f8c124e64e421893275ccdc83818ad`의 stdout, 세션/최신 status, peer_reports의
  마지막 종료 보고, raw metadata를 대조했다. 모두 15:35 장 마감 종료와 일치한다.
  accepted=committed 콜백 43,174,550건, 최종 레코드 43,218,720건이며 queued/in_flight/
  pending/drop/write_failures=0, writer_closed=true, finalization이 일치한다.
  저장된 해시는 대조만 했으며 원문 전체 해시·스캔은 수행하지 않았다.
- 본체 10188/실행기 7816은 OS 프로세스 목록에서 부재했다. 로그 감시 7900은 PowerShell
  EncodedCommand를 풀어 해당 stdout의 UTF8 Get-Content -Wait임을 재확인한 뒤 종료했고,
  세 PID 모두 부재를 확인했다. 수집기 강제 종료·재시작·로그인·PC 종료는 하지 않았다.
- 첫 수신 08:30:02, 마지막 15:32:50, 장중 임계 침묵 0건, 최대 대기 469건이다.
  메모리 이력 454개가 남았고 마지막 commit 약 111 MiB, peak_commit 약 1,594 MiB다.
  정상 종료나 메모리 수치로 어제 충돌 원인·주소 공간/메모리 훼손 가설을 배제하지 않는다.
- 닫힌 raw를 mode=ro로 열어 seq 1,000,000/21,000,000/42,000,000부터 각 1,000건만
  인덱스 조회했다. 체결 1,023건 모두 signed_volume이며 FID15 양수 578/음수 445,
  무부호·0·비정상은 이 표본에서 0건, 방향 라벨과 부호 일치 1,023건이다.
  체결 가격 부호 있음 1,012/없음 11건이다. 실시간 타입은 주식체결 1,023/주식호가잔량
  1,973건이며 나머지 4건은 품질 기록(out_of_range_fid_41 3건, fid_51 1건)이다.
  방향 확인이 다른 품질 오류를 없애지 않았음을 확인했다. 전수 정확성·무누락·venue/NXT
  coverage 인증은 아니며, 오늘 기본 정규장 세션은 애프터마켓 전환 실측이 아니다.
- 상세 관측은 로컬 `operations_state/after_close_20260918.json`에 남겼다(비추적).
  원본 raw/Daily_baseline/old_data는 보존했다. 전체 DB 집계·백테스트는 하지 않았다.
- HEAD `25b5df7`에서 기존 전환 수정 코드와 런북을 대조하고 관련 두 테스트 파일을 재실행해
  114개 통과(8.23초)를 확인했다. 세 결함은 이미 해결되어 중복 수정하지 않았다.
  다음 작업은 명시적 전환 CLI/트리거의 오프라인 준비와 별도 승인된 소규모 실측이다.
  실제 전환·추가 로그인·운영 적용은 이번 예약에서 하지 않았다. 새 예약도 만들지 않았다.

아래 운영 관측과 15:40 전 미확인 기록은 당시 경과이며, 오늘 저장 종료의 현재 판정은 위를 따른다.

## 이번 완료 — 오프라인 전환 결함 세 가지

- 시작 HEAD `f9cb658`, 작업 트리는 깨끗했다. 아래 과거 검토의 세 결함은 현재 코드에서도
  남아 있었고, 먼저 추가한 회귀 3개가 모두 실패했다. 이후 코드와 가짜 Qt/OCX 회귀로 수정했다.
- 수신 공백은 콜백 진입 때 잡은 동일 monotonic 초로 계산한다. UTC/KST 표시는 별도 필드다.
  FID 추출 지연을 주입해도 정확히 7.25초이며, 미수신은 null이다. 체결/호가 침묵 시계는 유지했다.
- 전환 이후에는 애프터 구간의 1~300초 타이머만 적용한다. 정규장 duration이 만료됐어도
  새 구간을 조기 종료하지 않고, 실제 통계 루프에서 애프터 상한에 도달할 때 종료를 요청한다.
- 진단을 `operations_state/session_transitions/<transition_id>.json`에 단계 시작·완료·실패마다
  동기화/원자 교체한다. 필수 기록 실패 후 새 세션 생성·등록을 진행하지 않으며, 최종 기록 전에는
  입력을 열지 않는다. 실패·취소·시간 초과 시 열린 writer 정리와 결과를 기록한다.
  닫힌 정규장 raw/보고는 다시 쓰지 않고, 메모리 이력도 새 세션 경로로 교체한다.
- 세부 계약·한계는 [수집 안내의 단일 로그인 전환 절](docs/COLLECTION_RUNBOOK.md)에 둔다.
  CLI 자동 전환 트리거·장시간 운영·재로그인 기능은 추가하지 않았다.
- 검증: `tests/test_session_transition.py`와 `tests/test_collector_plan_wiring.py`의 112개 통과
  (9.60초), 이어 추가한 새 writer 생성 후 계획 기록 실패/실제 drain 대기 실패 2개 통과(0.98초).
  `test_live_collector.py`, `test_tick_collector_shutdown.py`, `test_queue_control.py`,
  `test_kiwoom_session_monitor.py`의 관련 기존 53개 통과(8.08초). 총 서로 다른 167개이며,
  마지막 종료 요청 처리 변경 후 앞의 두 파일 23개도 재확인했다(2.51초, 중복 집계 안 함).
  모두 임시 데이터와 가짜 Qt/OCX 검증이며 전체 테스트·부하 측정은 하지 않았다.

## 운영 관측과 동시 작업

- 12:10 KST 짧은 읽기 전용 관측: 세션 `21f8c124e64e421893275ccdc83818ad`, 본체 10188/
  실행기 7816의 명령줄·07:55:04 시작 시각을 상태 identity와 대조했다. 로그 감시 7900도 같은
  시작 시각과 PowerShell 명령줄이었다. 상태는 running/accepting, writer_closed=false,
  finalization=null, 오류/드롭 0이었다. 로그 수신 증가를 확인했으며 저장 완료 판정은 아니다.
- 앱의 다른 Stock 작업은 대기 상태였고, 기존 `automation-2`의 15:40 마감 정리 예약을 확인했다.
  예약을 생성·변경하지 않았다. 예약은 최신 이 절을 먼저 읽고 **세 결함 수정은 반복하지 않는다.**
- 운영 수집기 중단·재시작·교체, 추가 로그인·실제 구독 변경·주문, 운영 DB 조회/전체 스캔은
  하지 않았다. 원본 raw/Daily_baseline/old_data와 운영 환경을 보존했다.

## 미확인과 다음 작업

1. 15:40 기존 예약에서 동일 identity의 실제 종료 로그·상태·보고·OS 프로세스를 대조한다.
   15:35 예정 시각이나 파일 존재만으로 종료를 확정하지 않는다. 감시 창 정리도 해당 예약 범위다.
   **2026-09-18 14:2x KST 사전 대조**: 예약 시각(15:40) 전이라 종료 자체는 아직 확인 대상이
   아니다. 그 전 기준선만 읽기 전용으로 확인했다 — PID 10188(본체)/7816(실행기)/7900(로그 감시)
   모두 2026-09-18 07:55:04 시작으로 살아 있고 명령줄이 이전 관측과 동일했다. `capture_status.json`
   /세션 폴더 `status.json`은 state=running, accepting=true, writer_closed=false, finalization=null,
   error=null, dropped=0이었다. 이 세션은 `server: mock`이고 애프터마켓 계획 없이 시작됐다 —
   즉 오늘 정규장 실행 자체는 세션 전환 실측에 그대로 이어 쓸 수 없다(별도 준비 필요, 아래 참고).
   실제 종료 로그·프로세스 부재 대조는 15:40 이후에만 가능하며 아직 미확인이다.
2. 실제 OCX 전환·수신 공백·무누락·장시간 안정성은 미검증이다. 30초 제한은 단계/기록 경계 검사이며
   네이티브 OCX나 OS I/O의 강제 중단 보장이 아니다. 디스크 장애 시 실패 기록 저장도 미확인일 수 있다.
3. 실측에는 현 수집의 정상 종료/프로세스 부재, 사용자 로그인 가능 상태와 별도 운영 적용 요청,
   확인된 소수 NXT 대상 목록, 같은 로그인 안의 정규장→별도 애프터 세션, 애프터 1~300초 상한이
   필요하다. 실제 반환값·진단 단계·양쪽 저장 종료·수신 공백을 대조하며 venue/coverage는 미확인 유지한다.
   실행 조건·관측 항목을 [수집 안내의 실측 조건과 관측 항목 정리](docs/COLLECTION_RUNBOOK.md)에
   구체화했다 — CLI 스위치가 없어 임시 스크립트로 직접 생성해야 하는 점, mock/계획 없는 세션은
   재사용 불가한 점을 포함한다. 정리만 했고 실행하지 않았다.
4. 코드 회귀 재확인(중복 구현 아님, 대조용): HEAD `11d3ac8` 작업 트리 깨끗함. `.venv`로
   `tests/test_session_transition.py`+`tests/test_collector_plan_wiring.py` 재실행 114 통과
   (10.41초) — 기존 보고(112+2)와 일치, 회귀 없음. CLI에 `--aftermarket` 류 스위치가 여전히
   없음을 grep으로 재확인(런북의 "아직 구현하지 않은 것"과 일치). 실제(비합성) `session_transition_v2`
   기록이나 `subscription_plan.json`은 `operations_state/` 어디에도 없음 — 이 전환 경로가 테스트
   더블 밖에서 실행된 적이 아직 없음을 뒷받침한다.

아래 기록은 이전 작업의 경과다. 세 결함의 현재 상태는 위 완료 항목을 우선한다.

## 전체 커밋 메시지 한국어화

- 사용자 명시 승인으로 공유 main/master의 전체 이력 134개를 재작성했다. 메시지 변경은 100개이며
  한국어인 메시지와 공동 작성자 표기는 보존했다. 모든 커밋의 파일 tree·작성자/커미터·시각과
  병합 부모 대응 관계 134개를 대조했다. 코드·수집 프로세스·raw 데이터는 변경하지 않았다.
- 이전 ID와 새 ID 대응은 [커밋 대응표](docs/COMMIT_ID_MAP_20260918.json)에 있다.
  기존 문서·수집 메타데이터의 리비전은 당시 원본 ID로 유지한다. 원본 이력은 로컬
  refs/backup/korean-messages-20260918/main 및 master와 operations_state/history_korean_20260918에 보존한다.
- 원격 갱신은 승인된 두 브랜치만 예상 원격 ID를 지정한 --force-with-lease와 --atomic으로 수행한다.
  다른 PC의 이전 체크아웃은 사용자 변경을 백업한 뒤 새 이력을 기준으로 연결해야 한다.
  이전 브랜치를 그대로 병합하면 영어 원본 이력이 재유입될 수 있다. 강제 초기화는 실행하지 않았다.

## 협업 방식 갱신

- 사용자 요청으로 앞으로 커밋 제목·본문을 한국어로 작성하도록 AGENTS.md에 명시했다.
  이후 전체 이력 변경 승인에 따라 위의 한국어화와 커밋 대응표를 반영했다.

- 사용자 요청으로 AGENTS.md에 Codex/Sonnet 5 인계 규칙을 추가했다. 다른 AI의 결과 전달 시
  현재 근거를 대조하고 다음 작업용 복사 가능한 프롬프트를 대화에 제공한다.
- Codex 직접 구현·분석 요청을 우선하며, 별도 인계 파일은 요청 없이 추가하지 않는다.
  검증된 변경의 커밋·푸시·fast-forward 병합은 원격 상태 확인 후 재확인 없이 처리한다.

## 21956af Windows 독립 검토 — 실측 전 수정 필요

- 전환/수집기 연결 합성 54개 통과(1.70초). 그러나 다음 계약 결함이 코드 대조에서 확인됐다.
  운영 로그인이나 수집을 실행하지 않았다. 아래는 수정 완료 기록이 아니다.
- 수신 공백 시계 불일치: run_aftermarket_transition은 SessionMonitor.last_event_ts(epoch)를
  last_event_before로 전달하지만 note_first_event_after는 monotonic을 쓴다. 차이가 잘못된 음수가
  될 수 있다. 같은 단조 시계의 실제 수신 시각을 별도로 유지하고 UTC/KST 시각을 분리해야 한다.
  현재 연결 테스트는 gap이 None이 아닌지만 확인해 이 오류를 잡지 못한다.
- 전환 후 정규장 duration_seconds가 살아 있다. 애프터 제한이 아직 미도달이면 elif의 정규장
  경과 시간 조건을 검사하므로 이전 시간이 만료된 경우 애프터를 조기 종료한다.
  전환 전/후 타이머 소유를 분리하고 실제 통계 루프로 반례를 검증해야 한다.
- 전환 기록은 전체 단계 실행 뒤에만 파일로 쓴다. 단계 중 크래시 시 기록이 없고 최종 기록 실패도
  이미 다음 세션 생성/등록을 진행한 뒤 발생한다. 단계별 지속 기록 성공을 다음 단계의 전제로
  만들고 기록 실패·취소·시간 초과 주입 테스트를 보완해야 한다.
- 따라서 CLI 트리거 추가나 실제 OCX 검증보다 이 계약 보완을 우선한다.

## 2026-09-18 — `ed3a680` 전환 구현 감사, 실제 결함 수정 (가짜 OCX, 운영 미적용)

이전 세션이 "813 통과"로 보고한 `ed3a680`(정규장→애프터마켓 전환)을 완료로 믿지 않고
코드와 테스트를 대조했다. 스펙(전환 계약·상태 기록·전환 중 콜백 진단)과 실제 구현 사이에
실질적인 격차 여러 개를 발견해 고쳤다. 코드·문서·합성 테스트만 다뤘고 실제 로그인/수집은
하지 않았다.

### 발견하고 고친 것

- **가장 심각한 결함 — 애프터마켓 구간이 무제한으로 돌 수 있었다.** `aftermarket_plan` 을
  받는 생성자가 `duration_seconds` 를 전혀 검증하지 않았다. 정규장 15:35 종료는 계획 모드에
  적용하지 않으므로, 전환 뒤에는 종료 조건 자체가 없어 20시는커녕 무한정 실행될 수 있었다.
  `aftermarket_duration_seconds`(1~300초, 생성자에서 필수)를 추가하고, `subscribe_nxt` 성공
  시각부터 별도로 재는 상한을 `_stats_worker` 루프에 연결했다. 정규장 `duration_seconds`/로그인
  시각을 그대로 이어받지 않는다 — 이어받으면 전환 전에 이미 만료된 값으로 애프터마켓을
  곧바로 끊을 수 있기 때문이다.
- **전환 기록에 스펙이 요구하는 필드가 없었다.** 전환 식별자, 이전/다음 `session_id`, 코드
  리비전, 계획 원문, 단계별 시작·완료 시각(단조 시계 + UTC + KST), 단계 반환값/예외가 전혀
  없었다(기존 필드는 전체 시작/종료 시각과 실패 단계뿐). schema `session_transition_v1` →
  `v2`로 모두 추가했다.
- **전환 중 콜백 진단이 코드/타입만 남기고 있었다.** 등록된 FID 원문(GetCommRealData 로 읽을
  수 있는 것만이며 서버 전체 제공 필드라는 뜻은 아니다), 콜백이 도착한 순간의 전환 단계,
  활성 구독 문맥(참고 정보일 뿐 실제 출처 확정이 아니라는 점을 note 로 명시)을 추가했다.
  보존에 상한(기본 2,000건)을 두고, 넘으면 건수를 세고 조용히 버리는 대신 전환 자체를
  `callback_capacity_exceeded` 로 실패 처리한다.
- **`note_first_event_after()` 를 실제로 아무도 호출하지 않았다.** 유닛 테스트에서만 직접
  호출됐을 뿐, 수집기 연결에는 없어 실제로는 첫 애프터마켓 수신 시각과 수신 공백이 항상
  미확인으로 남았다. 전환 뒤 첫 accepted 콜백에서 호출하고, 이미 써 둔 `session_transition.json`
  을 다시 갱신하도록 연결했다.
- **전환 뒤에도 정규장 감시기(SessionMonitor) 인스턴스를 그대로 썼다.** 애프터마켓 시장
  프로필이 적용되지 않고 정규장의 마지막 수신 시각·카운터를 이어받을 수 있었다 — 런북이
  이미 지적한 "새 세션에는 새 감시 상태를 쓴다"는 원칙 위반이다. `open_aftermarket` 단계에서
  애프터마켓 프로필로 새 `SessionMonitor` 인스턴스를 만들어 교체하도록 고쳤다.
- `SetRealRemove`/`SetRealReg` 반환값을 단계 함수가 버리고 있어 성공 시에도 기록에 남지
  않았다. 반환값을 그대로 돌려주게 해 단계별 기록(`outcome`)에 남게 했다.
- 관련 합성 테스트 13개 추가(`session_transition` 7, 수집기 연결 6). `tests/` 디렉터리 전체
  1008 → 1021 통과, 실패 0(변화 없음, 회귀 없음). 저장소 루트 전체 실행은 이 환경에 32비트
  PyQt5(`collector/kiwoom/test_login.py`)가 없어 애초에 수집이 안 된다 — 이 환경 제약이며
  이번 변경과 무관하다.
- `docs/COLLECTION_RUNBOOK.md`의 정규장→애프터 전환 절이 "아직 구현하지 않았다"는 문구를
  구현이 끝난 뒤에도 그대로 담고 있었다. 실제 구현·기록 스키마·종료 조건·남은 항목으로 갱신했다.

### 아직 실제 OCX에서 검증하지 않은 것

- 네 단계 전체, 특히 `SetRealRemove`/`SetRealReg` 의 실제 반환값, 전환 실제 소요 시간,
  실제 수신 공백 길이. 모두 가짜 OCX 합성 검증으로만 확인했다.
- 애프터마켓 시장 프로필로 교체된 감시기의 실제 침묵/저활동 판정.
- 전환 중 콜백 진단의 실제 FID 원문 캡처(등록되지 않은 FID·읽기 실패 시 동작 포함).

### 다음 소규모 실측의 실행 조건

- **CLI 트리거가 아직 없다.** 지금은 Python에서 `KiwoomUniverseLogger(aftermarket_plan=...,
  aftermarket_duration_seconds=1..300)` 를 직접 생성하고 `run_aftermarket_transition()` 을
  호출해야 돈다. 실측 전에 최소한의 CLI 스위치(또는 스크립트 러너)가 있어야 사람이 재현할 수 있다.
- 실측은 애프터마켓 구간 1~300초 제한을 유지한 채, 소수 `_NX` 종목으로 진행한다.
- 로그인 상태·정규장 저장 정상 종료를 먼저 확인한 뒤에만 전환을 호출한다.

### 장시간 운영 전에 남은 항목

- CLI에서 이 모드를 선택하는 인자, 그리고 정규장 종료 시각(15:35)이나 경과 시간을 기준으로
  자동으로 `run_aftermarket_transition()` 을 호출하는 시계 트리거 — 둘 다 아직 없다.
- 20시까지의 장시간 애프터마켓 운영 시 저장량·메모리 추이는 여전히 미측정이며, 지금의
  1~300초 상한을 없애는 것은 별도 결정과 실측 이후의 일이다.
- 별개 프로세스 재로그인은 전환 실패 시 사용자가 선택하는 수동 복구 수단으로 유지한다.
  자동 재시도·자동 재로그인은 요구되지도, 구현되지도 않았다.


## 18:3x KST — 단일 로그인 내 애프터마켓 세션 전환 (가짜 OCX, 운영 미적용)

전환 순서는 지정된 네 단계다. **정규장 저장이 닫혔다고 확인된 뒤에만** 다음 세션을 연다.

1. 정규장 구독 해제·신규 입력 차단 → 2. 큐 저장 완료·파일 닫힘·마무리 정보 확인
→ 3. 새 애프터마켓 저장 세션 생성 → 4. NXT 구독 등록·수신 시작

- **어느 단계든 실패하면 다음으로 넘어가지 않고 실패 위치를 기록한다.** 2단계가 예외 없이
  거짓만 돌려줘도 실패로 본다 — 닫혔는지 모르는 채로 다음 파일을 열지 않기 위해서다.
  단계별 실패를 주입해 후속 단계가 호출되지 않는 것을 각각 확인했다.
- **닫힌 정규장 파일은 다시 열거나 고치지 않는다.** 전환기는 정규장 저장을 쓰기용으로
  들고 있지 않고 2단계 이후에는 확인 결과만 읽는다. 전환 뒤 수신이 새 파일에만 쌓이고
  정규장 파일에는 전환 전 수신만 남는 것을 양쪽 파일을 읽어 대조했다.
- **전환 중 도착한 콜백을 버리지 않는다.** 저장 파일에는 자리가 없으므로 전환 기록에
  원문을 보존하고, 세션 폴더의 `session_transition.json` 에 건수와 원문이 함께 남는다.
- 전환 시간과 수신 공백(마지막 정규장 수신 → 첫 애프터마켓 수신)을 기록한다.
  **무누락은 보장하지 않는다** — 해제와 등록 사이 공백은 반드시 생기고 길이를 약속할 수 없다.
  기록에도 그 문구를 넣었다.
- **자동 전환은 명시적 모드다.** 전환 계획을 넘길 때만 준비되고, 기본은 전환 없음이다.
  정규장 계획과 전환 계획의 동시 지정, NXT 아닌 모드, 관리 실행·raw-v1은 거부한다.
  기존 1~300초 제한 실행은 그대로다.
- 검증 중 결함 하나를 잡았다. 전환 코드가 저장 세션 클래스를 함수 안에서 다시 import 해
  로그인 경로가 쓰는 모듈 참조를 우회했다. 생성 지점이 둘로 갈리고 대역 주입도 막혔다.
  모듈 참조를 쓰도록 고쳤다.
- 전환 단위 12개, 수집기 연결 29개. 전체 813 통과(직전 796), 실패·오류 31/37 변화 없음.

### 남은 것

- 실제 로그인에서의 전환 실측. 전환 소요 시간과 실제 수신 공백 길이는 측정된 바 없다.
- 전환 시각을 무엇으로 정할지(시계 기준 자동 발동)는 구현하지 않았다. 지금은 호출해야 돈다.
- 별개 실행 재로그인은 전환 실패 시 수동 복구 수단으로 남겨 둔다.
- 호가 침묵 기준, 600초 임계 타당성, 장시간 안정성과 저장량은 여전히 미측정.


## 18:0x KST — CLI 계획 입력과 계획 파일·콜백 저장 통합 검증 (로그인 없음)

`3e41744`의 제약(독립 raw-v2·1~300초) 위에 이어 붙였다. 가짜 Qt/OCX 로만 확인했다.

- **명령줄에서 계획을 만든다.** `--nxt-codes`, `--list-origin`, `--list-verified-at`,
  `--market-profile`, `--nxt-eligibility-confirmed`, `--list-note` 를 추가했다.
  파싱을 `__main__` 인라인에서 순수 함수로 빼내 테스트 가능하게 했다.
  목록 근거는 선택 항목이 아니다 — 출처나 확인 시각이 비면 계획을 만들 수 없다.
  `--nxt-eligibility-confirmed` 는 **사용자가 직접 밝힐 때만** 참이고 기본은 거짓이다.
  근거 인자만 주고 `--nxt-codes` 를 빼면 거부하고, `--codes` 와 동시 지정도 거부한다.
  제한 시간 1~300초는 수집기 생성 전에 명령줄에서 먼저 확인한다.
- **계획 파일이 실제로 쓰이는 것을 확인했다.** 로그인 경로를 태운 뒤 세션 폴더의
  `subscription_plan.json` 을 읽어 모드·구독 코드 원문·시장 프로필·목록 출처·확인 시각·
  대상 확인 여부(거짓)·venue 미확인·coverage 미확인이 모두 담기는지 대조했다.
- **콜백 코드의 접미사가 저장까지 보존된다.** `005930_NX` 체결과 `000660_NX` 호가를 흘려
  raw 레코드의 코드가 접미사째 남고 venue 는 여전히 unknown 인 것을 확인했다.
  구독하지 않은 코드가 도착해도 조용히 버리지 않고 원문을 남긴다 — 대조는 기록을 보고 한다.
- 계획 모드에서도 저장이 정상 종료(closed)되는 것을 확인했다.
- 수집기 연결 검증 24개, 구독 계획 22개. 전체 796 통과(직전 777), 실패·오류 31/37 변화 없음.

### 남은 것

- **세션 전환은 여전히 없다.** 계획 모드는 15:35에 멈추지 않을 뿐, 정규장 세션을 마무리하고
  애프터마켓 세션으로 넘기는 동작은 구현하지 않았다. 지금은 제한 시간으로 끝나는 독립 실행이다.
- 실제 로그인·수신·저장·종료 확인, 장시간 안정성, 호가 침묵 기준과 600초 임계의 타당성은 미측정.
- NXT 거래 대상 목록의 자동 확보 수단 미정.


## fe38c07 독립 재검증 — 무제한 계획 실행 차단

- 전달받은 시장 구간·구독 계획·수집 연결 코드를 Windows에서 확인했다. 계획 모드는
  15:35 종료를 제외하면서 대체 종료를 필수로 요구하지 않아 무제한 실행 가능성이 있었다.
- NXT 계획은 독립 raw-v2와 명시적 정수 1~300초 제한 실행만 허용하도록 보강했다.
  raw-v1·관리 실행과 조합, 시간 누락/0/음수/범위 초과/비정수는 OCX 생성 전에 거부한다.
- 관련 Windows 합성 109개 통과(1.87초). 실제 통계 루프가 제한 시간 도달 시 종료를 요청하는
  회귀를 포함한다. 로그인·운영 기동은 하지 않았고 장시간 실측을 대신하지 않는다.
- 현재 계획 연결은 생성자 인자 수준이다. 일반 CLI는 계획을 받지 않으므로 이 연결만으로
  기존 실행 명령에서 NXT 모드를 선택할 수 없다. CLI 계획 입력과 계약 검증을 다음에 구현한다.
- 계획 기록 테스트는 describe/로그 중심이다. 실제 로그인 대역에서 subscription_plan.json 파일
  저장과 콜백 원문의 연계를 확인하는 통합 검증도 남는다. 오늘 완료로 확대 해석하지 않는다.
- 장시간 모드의 시장 종료/저장 마무리 및 정규장→애프터 세션 전환은 여전히 미구현이다.

## 17:3x KST — 운영 수집기에 구독 계획 연결 (가짜 OCX 검증, 실제 기동 없음)

로그인 없이 가짜 Qt/OCX 대역으로만 확인했다. 실제 수신·저장·종료 확인은 다음 실행 단계다.

- **명시적 NXT 모드에서만 계획을 받는다.** 정규장 모드 계획을 넘기면 거부하고,
  계획과 `--codes` 를 함께 주는 것도 거부한다. 계획이 없으면 기존 전 종목 경로가 그대로 돈다.
- 계획이 있으면 **유니버스 조회를 아예 하지 않는다.** 접미사 코드는 조회로 받는 여섯 자리
  목록에 없으므로 기존 '관측된 보통주에 있는가' 검사를 적용할 수 없고, 그 목록을 NXT 대상
  목록으로 대신 쓰지도 않는다. 등록은 계획의 코드만 원문 그대로 보낸다(`005930_NX;000660_NX`).
- 등록 FID 목록을 상수로 뽑아 전 종목 경로와 계획 경로가 **같은 목록**을 쓰도록 했다.
- 세션 폴더에 `subscription_plan.json` 으로 **목록 출처·확인 시각·NXT 대상 확인 여부·시장
  프로필·구독 코드 원문**을 남긴다. 로그에도 출처와 "미확인 — 사용자 입력일 뿐이다" 를 찍는다.
  기록에는 venue 미확인과 coverage 미확인이 항상 함께 남는다.
- 감시기는 계획의 시장 프로필 구간을 쓴다. 계획이 없으면 기존 단일 창 그대로다.
- **계획 모드에서는 15:35 정규장 마감 종료를 적용하지 않는다.** 15:35 를 20:00 으로 미루는
  것으로는 부족하다는 런북 지적에 따라, 정규장 전용 종료 조건에 계획 부재를 함께 건다.
- **체결 전용 시계가 경고와 600초 종료 판단 모두에 쓰인다.** 호가만 50초 간격으로 들어오는
  동안 체결 10분 무수신이면 종료 권고가 나오는 것을 수집기 수준에서 확인했다.
- 가짜 OCX 검증 11개: 등록 인자(화면번호·코드 문자열·FID·옵션), 유니버스 조회 미수행,
  모드 제한, 계획/코드 동시 지정 거부, 근거 기록, 프로필 연결, 체결 전용 시계 양방향,
  등록 거부 시 예외 전파, 정규장 경로 불변. 전체 777 통과(직전 766), 실패·오류 31/37 변화 없음.

### 남은 것

- 실제 로그인·구독·수신·저장·종료 확인. 소수 종목 제한 실행으로 해야 한다.
- 애프터마켓 세션으로의 저장 전환은 구현하지 않았다. 계획 모드는 15:35에 안 멈출 뿐,
  정규장 세션을 마무리하고 새 세션으로 넘기는 동작은 없다.
- NXT 거래 대상 목록의 자동 확보 수단 미정. 호가 침묵 기준과 600초 임계의 타당성도 미측정.


## 17:0x KST — 시장 구간 감시 연결과 구독 계획 구현 (로그인 없음, 운영 미적용)

로그인이나 수집기 실행 없이 코드와 합성 검증만 했다. 실제 구독·기동은 별도 실행 단계로 남는다.

### 시장 구간 감시 연결

- 침묵 판정이 창 하나(09:00~15:30)에 묶여 있던 것을 거래소별 구간으로 나눴다.
  **기본 경로는 그대로다** — 프로필을 넘기지 않으면 기존 단일 창 코드가 그대로 돌고,
  명시적으로 NXT 프로필을 줄 때만 구간별 판정으로 바뀐다. 두 경로를 같은 테스트에서 대조한다.
- NXT 메인 개장은 **09:00:30** 이다(앞선 보고의 09:00은 오기였다).
- **체결과 호가의 기준을 분리했다.** 구간마다 종류별로 세 상태를 갖는다 — 판정할 임계값,
  기대하지 않음(조용한 것이 정상), 판정하지 않음(근거 없음). 15:20~15:40 전환 구간은
  체결 기대 밖이지만 호가 수신 여부는 확인된 바 없어 판정하지 않음으로 둔다.
  호가는 현재 전 구간에서 판정하지 않는다. **이 판정은 수신·저장을 막지 않는다.**
- **침묵은 열린 구간 안의 시간만 센다.** 닫힌 구간을 가로지르면 누적하지 않고 다음 구간이
  열린 시각부터 다시 잰다. 15:10 마지막 수신 후 15:45 현재는 35분이 아니라 5분이고,
  밤을 넘긴 침묵은 전날이 아니라 당일 구간 개장부터 잰다.
- 600초는 **임시 정책값**이며 실측으로 검증되지 않았다. 저활동 표시와 별개인 플래그로 표시한다.
- **호가 수신이 체결 침묵을 초기화하지 않는다.** 종류별 마지막 수신 시각을 따로 들고 간다.
  이전 구조는 시각을 공유해, 호가가 계속 오면 체결이 끊겨도 경고가 나오지 않았다.
  호가만 50초 간격으로 들어오는 동안 체결 10분 무수신을 잡는 테스트로 고정했다.

### 구독 계획

- 접미사 허용을 **전역으로 풀지 않았다.** 정규장 모드는 여섯 자리만 받고 접미사를 거부한다.
  `_NX` 는 명시적 NXT 모드에서만 허용한다. 통합 `_AL` 은 체결 거래소를 단정할 수 없어
  운영 구독에서 거부하고 비교 검증 전용으로 남긴다.
- **목록 출처와 확인 시각이 필수다.** 둘 중 하나라도 비면 계획을 만들 수 없다.
  그리고 '사용자가 입력했다' 와 'NXT 거래 대상임을 확인했다' 를 **별도 항목으로** 구분하며
  후자의 기본값은 미확인이다. 조회로 받는 여섯 자리 목록을 NXT 대상 목록으로 간주하지 않는다.
- 종목 수는 1~10개로 제한한다. 전 종목 확장은 별도 검증 뒤의 일이다.
- 구독 코드 원문·콜백 코드·시장 프로필을 기록하되 **접미사 일치만으로 venue 나 coverage 를
  올리지 않는다.** 기록에는 항상 venue=unknown, 거래소 미확인, coverage 미확인이 함께 남는다.
- 합성 검증: 잘못된 접미사, 중복 코드, 목록 근거 누락, 정규장 호환성, 소수 종목 제한,
  접미사 불일치 기록까지 16개. 시장 구간 20개. 관련 묶음 전체 통과.
  전체 766 통과(직전 748), 실패·오류는 31/37로 변화 없다(이 환경의 pyarrow/streamlit 부재와
  Windows 전용 경로).

### 아직 아닌 것

- 운영 수집기에 구독 계획을 연결하지 않았다. 실제 `_NX` 구독·기동은 로그인이 필요한 별도 단계다.
- NXT 거래 대상 목록의 자동 확보 수단은 미정이다. 당분간 사람이 확인해 넣은 소수 종목으로 시작한다.
- 정규장 종료 후 애프터마켓으로의 저장 세션 전환은 구현하지 않았다. 자동 전환을 약속하지 않는다.
- 호가 침묵 기준, 애프터마켓 임계값의 타당성, 장시간 안정성과 저장량은 모두 미측정이다.


## 다음 작업자가 먼저 읽을 요약 — 완료와 남은 작업

이 요약이 아래 시간순 기록과 과거의 '다음 작업'보다 우선한다. 프로세스 상태는 재접속 시 다시 확인한다.

### 이번에 완료한 작업

- 오전 수신 정체와 네이티브 충돌을 분리해 조사했다. 안랩 모듈 null 함수 호출의 강한 근거를
  확보했지만 원인은 미확인이다. 부하·주소 공간·선행 메모리 훼손을 배제하지 않는다.
- 원문 보존, 방향 signed_volume 기존 구현 재검증, NXT 결손 가드 보강을 완료했다.
  다음 운영 raw-v2 기본 방향은 signed_volume이며 무부호·0·비정상은 미확인으로 남는다.
- 메모리 이력·600초 침묵 종료 시도·종료 직전 스택 기록을 추가했다. 실제 장애에서 안전 종료를
  보장하지 않으며 새 운영 세션에서 장시간 효과는 미검증이다. 관련 합성 81개 통과 기록이 있다.
- 오늘 정규장 수집 세 세션 중 마지막 오후 세션만 저장 종료를 확인했다. 하루 전체 무누락 인증은 아니다.
- 별도 모의 OCX 검증에서 삼성전자 6자리/_NX/_AL 각 60초의 체결·호가 828건을 원문과 대조했다.
  접미사 수신은 실측됐지만 전 종목·전체 시간대·개별 체결 venue/coverage는 미검증이다.
- 운영 애프터마켓 자동 수집이나 예약은 추가하지 않았다. 원본 raw/Daily_baseline/old_data는 보존했다.

### 사용자가 밝힌 데이터 수집 목적

- 정규장 개장 후 진입할 때는 당일 아침 NXT 흐름을 참고하려는 구상이다.
- NXT 아침 개장부터 진입할 경우 당일 누적 정보가 부족하므로 전날 애프터마켓을 비교 기준으로
  활용할 수 있게 보존하려는 목적이다. 저녁 매매를 시작하겠다는 요청이 아니다.
- '당일 아침만으로 충분한가 / 전날 저녁까지 쓰면 도움이 되는가'는 연구 가설이며 성과는 미검증이다.
  두 구간을 분리 저장하고 야간 공백을 연속 수신으로 취급하지 않는다.

### 다음 작업 순서

1. 다음 운영 실행 전에 코드 리비전·구독 범위·시장 시간대를 확인한다. 새 세션에서 signed_volume,
   메모리 이력, 종료 진단의 실제 적용을 확인하고 raw와 품질 오류 보존을 제한 표본으로 대조한다.
2. NXT 프리/애프터 구독 모드를 설계·구현한다. 접미사 허용과 대상 목록 검증, 원문 구독/응답 코드,
   시장별 시간대·침묵 감시, 정규장 종료와 별도 저장 세션 전환을 함께 다룬다.
   15:35만 20:00으로 바꾸지 않는다. 상세 계약은 [수집 안내](docs/COLLECTION_RUNBOOK.md)에 둔다.
3. 사용자가 로그인 가능한 시점의 소수 종목 제한 실측으로 프리마켓·애프터마켓 저장 종료와
   메모리/디스크 증가를 확인한다. 오늘 한 종목 결과를 전 종목 지원으로 확대하지 않는다.
4. 충분한 날짜가 모이면 원본 틱 재생으로 진입 시점별 비교를 준비한다. 당일 프리마켓만 사용하는
   기준과 전 거래일 애프터마켓까지 사용하는 기준을 동일 조건에서 비교하고 진입 뒤 데이터는 배제한다.
   휴장일·전 거래일 연결, 야간 공백·결손·시간대별 유동성을 구분한다. 성과 개선을 미리 가정하지 않는다.
5. 오늘 raw-v2 방향 파생 결과가 필요하면 별도 산출물로 재정규화한다. 과거 v1의 부호 소실을
   추정 방향으로 덮어쓰지 않는다. 기존 결손·venue 미확인·coverage 가드는 유지한다.


## 후속 요청 — 애프터마켓 수집 검토

- 사용자가 모의 NXT 검증 요약의 기존 GitHub 원격 푸시를 명시적으로 승인했다.
- 장 종료 후 애프터마켓 수집도 검토하도록 요청했다. 런북에 제한 종목 `_NX`, 별도 저장 세션,
  단일 로그인 내 전환 검토, 15:40~20:00 시간대/침묵 감시 및 단계적 확대안을 남겼다.
- 현재 15:35 종료 시각만 늦추는 것으로는 부족하다. 운영 수집기의 접미사 검증과 정규장 전용
  침묵 시간대도 함께 다뤄야 한다. 추가 실행/예약·운영 변경은 하지 않았다.

## 16:04 KST — 모의 OCX 접미사 구독 실측 완료

- 최초 시도는 login_timeout/0건으로 종료했다. 사용자 요청으로 16:01 재실행 후 모의 플래그 1 확인.
  16:01:18~16:04:18에 삼성전자 세 코드 각 60초 순차 관측, 등록 반환은 모두 0이었다.
- 원문 JSONL 재집계와 result.json 일치: 005930 체결 9/호가 195,
  005930_NX 체결 106/호가 197, 005930_AL 체결 124/호가 197. 합계 828건.
  각 콜백 코드가 해당 접미사를 유지했다. 모의 OCX SetRealReg의 해당 종목·구간 접미사 수신을 확인했다.
- observation_windows_completed, exit_code 0, removal_error null, stderr 공백 및 실행기 PID 18248 부재 확인.
  원문/결과: operations_state/nxt_probes/5561eb702ca2409fa4fe7ee2dd45159a/.
  로그: operations_state/nxt_probe_20260917_160104.*.log. 원본 수집 DB는 변경하지 않았다.
- 순차 구간이므로 코드 간 건수는 동일 시각 비교가 아니다. 개별 체결 실제 거래소의 독립 증명,
  전체 NXT 종목·구간 coverage, 모의/실전 동일성은 미확인이다. venue/coverage 인증은 올리지 않는다.
- 다음 작업은 운영 구독 옵션/시간대 설계와 별도 제한 검증이다. 전 종목 NXT 운영 전환은 하지 않았다.

## 15:53 KST — 사용자 요청 애프터마켓 소규모 검증 시작, 로그인 대기

- 기존 Python 수집 프로세스 부재 확인 후 별도 `run_nxt_probe`를 실행했다. 전 종목 수집기는 재실행하지 않았다.
  삼성전자 005930 / 005930_NX / 005930_AL 각 60초 순차 관측, 모의서버 확인 후에만 등록한다.
- 실행기 PID 10784, stdout/stderr `operations_state/nxt_probe_20260917_155301.*.log`.
  결과 폴더 `operations_state/nxt_probes/a4bdd02587b44111afc8449b1e1933d8`.
  이 기록 시점에는 로그인 대기이고 실제 구독·NXT 수신은 미확인이다. 로그인 대기 180초 후 자동 종료한다.
- 모의서버 제한·원문/접미사 보존·순차 종료·로그인 시간 제한 합성 12개 통과,
  별도 32비트 PyQt/검증 모듈 import 성공. 실측 완료 후 result/원문/프로세스를 대조해야 한다.
- 관측 기록의 활성 구독 코드는 전환 직후 지연 콜백의 출처를 인증하지 않는다.
  구독 반환 성공·0건만으로 NXT 지원/미지원이나 개별 체결 거래소를 확정하지 않는다.

## 마감 보고 독립 대조 및 다음 실행 방향 정책

- 마지막 세션 상태·종료 로그에서 accepted=committed=15,687,055, pending/dropped=0,
  writer_closed=true와 finalization을 대조했고 PID 7804/12888은 부재다.
  전체 DB 해시 재계산·전체 무누락 검증은 하지 않았다.
- 읽기 전용으로 seq 1,000 / 10,000,000 / 19,000,000부터 각 1,000행만 추가 조회했다.
  체결 575건의 FID 15는 양수 부호 246 / 음수 부호 329, 무부호·0·비정상 0이었다.
  근거는 operations_state/signed_volume_review_20260917.json. 비무작위 제한 표본이다.
- CP949 공식 OCX 가이드 2439~2442행과 대조하고 다음 새 raw-v2 세션의 기본 방향을
  signed_volume으로 변경했다. venue는 unknown, 가격 signed_magnitude는 유지한다.
  기존 raw를 수정하지 않았으며 새 로그인·수집 실행도 하지 않았다.
- 관련 합성 81개 통과(3.14초). 운영 연결에서도 무부호·0·비정상은 방향 미확인,
  다른 가격 오류는 유지됨을 확인했다. 실제 새 세션 적용 관측은 다음 실행 때 필요하다.
- 아래 10,384건/8.6%/21% 수치는 다른 작업의 보고이며 이 대조에서 재산출하지 않았다.

## 15:38 KST — 장 마감 종료 검증, 정규장 부호 실측, NXT 코드 차단 확인

- 오늘 세션은 셋이다. **마지막 세션만 정상 종료**했고 앞의 둘은 마무리 정보가 없다.

  | 세션 | 구간 | 저장 | 상태 | 마무리 정보 |
  |---|---|---|---|---|
  | 1b120bb4 | 07:27~11:30 | 14.36GB | 돌아가는 중에서 멈춤, 파일 안 닫힘 | 없음 |
  | e13522d5 | 11:35~12:05 | 3.07GB | 중단(중복 로그인), 파일은 닫힘 | 없음 |
  | cf18cb43 | 12:35~15:35 | 20.00GB | **정상 종료** | 있음 |

  마지막 세션 요약: 2시간 59분 14초, 체결 4,176,508 / 호가 11,510,547, 콜백 15,687,055건에
  레코드 19,881,597건, 장중 대기큐 최대 338건(15:17:39), 임계 초과 침묵 0건, 드롭·오류 0.
  마지막 수신 15:32:54로 종료까지 2분 7초 갭이나 장중 결손 판정은 0초다.
  해당 오후 세션의 저장 종료 확인이며 하루 전체 무누락·데이터 품질 인증은 아니다. 세 세션 합계 37.43GB로
  9/16 기존 방식 7.0GB의 5.3배다. 세 세션 모두 검증 파일은 없다(별도 진단 절차의 산출물이다).

- **정규장 체결 원문 실측** (마지막 세션에서 10구간 분할 표본 10,384건, 12:36~15:32).

  - 가격 항목에 **부호가 실려 온다**: 양수 7,511 / 음수 2,663 / 무부호 210. 예시 `-119900`, `+10785`.
    전일 대비 등락 부호이며 가격 자체의 부호가 아니다. 현재 절댓값 처리 정책이 맞다는 근거다.
    오전 장전 구간 표본만으로 "부호가 오지 않으니 양수 전용이 맞겠다" 고 본 잠정 판단은 틀렸다.
  - 거래량 항목은 **전 건 부호가 있다**: 양수 5,630 / 음수 4,754, 무부호 0건(0.000%).
    해당 표본의 방향을 판정할 근거이며 전체 체결의 방향 미확인 0건을 보장하지 않는다.
  - 최우선매도호가가 0인 체결은 7건(0.1%)뿐이다. 오전 장전 구간에서 전 건 0이던 것과 대비된다.

- **예전 방식 방향 판정을 오늘 원문에 재적용해 문서 명세와 대조했다 — 일치율 91.4%.**
  10,384건 중 일치 9,493, 불일치 891(8.6%). 내역은 문서상 매수인데 예전이 매도로 본 것 604건,
  문서상 매도인데 예전이 매수로 본 것 287건이다. 예전 판정은 거래대금 항목을 체결 구분으로
  오독한 데다 매도1호가 비교를 덧댄 규칙이었다. 기존 방향 라벨의 신뢰성 위험을 보여주지만
  기존 21GB의 오류율 또는 전략 성과 변화율을 8.6%로 추정할 수는 없다.
  주의: 오늘 하루 표본 1만 건 기준이고 다른 날의 일치율은 확인하지 않았다. 예전 저장은
  수신 즉시 절댓값을 씌워 부호를 버렸으므로 v1 파일 자체로 정확한 원문 방향 복원은 불가능하다.
  별도 신뢰 가능한 과거 원천과 대조할 가능성은 별개다. 오늘 raw-v2는 부호가 남아 있으므로
  원본을 보존한 별도 파생 재정규화가 가능하다. 향후 재수집으로 과거 결손이 복원되지는 않는다.

- 파싱 이슈는 체결 방향 미확인이 10,386건으로 사실상 전량이고, 호가 쪽은 29,208건 중 21건
  (최우선 호가가 0)뿐이다. 방향 정책을 켜면 제어 레코드의 99.8%가 사라져 표본 기준 레코드 수가
  약 21% 줄어든다. 저장 절감폭은 그보다 작다 — 사라지는 레코드가 체결 레코드보다 작기 때문이다.

- **NXT 소규모 구독 검증은 코드가 먼저 막는다.** 실행 전 단계에서 확인했다.
  명령줄이 종목코드를 "여섯 자리 숫자" 로만 받아 `005930_NX`·`005930_AL` 을 거부하고(569줄),
  등록 단계도 조회로 받은 6자리 목록에 없는 코드를 거부한다(294줄). 즉 접미사 코드는 증권사
  API에 닿기도 전에 우리 쪽에서 걸린다. 모의서버·실시간 등록 함수가 접미사를 받아주는지는
  **여전히 미확인**이며, 확인하려면 두 곳의 검증을 접미사 허용으로 고치고 로그인이 필요하다.

- 충돌 원인은 미확인이다. 안랩 모듈 널 호출은 충돌 지점의 증거일 뿐 부하·주소 공간·선행
  메모리 훼손을 배제하지 않는다. 거래소 구분 문제도 미해결이다.


## 13:11 KST — 메모리 이력·침묵 종료 코드 Windows 검증

- 다른 작업의 d5f0c5a를 확인했다. 600초는 임시 보수적 기본값이며 운영 검증으로 정한 값은 아니다.
  현재 세션에는 미적용이다. 재시작·추가 로그인 없이 코드와 작은 합성 테스트만 수행했다.
- 종료 요청 직전에 경고 덤프 예산과 별도로 스택 1회를 추가했다. 진단 실패도 종료 요청을 막지 않는다.
  Qt/OCX 정체 시 종료 경로 실행·파일 닫힘은 보장하지 않으며 자동 재로그인은 없다.
- Windows 관련 합성 52개 통과(1.65초), 별도 32비트 Python에서 OCX 없이 실제 메모리 조회 성공.
  전체 테스트와 실제 장애 재현·종료 검증은 하지 않았다. 수집 중 전체 검증은 진행하지 않는다.
- 안랩 모듈 null 호출은 충돌 지점의 강한 근거다. 선행 훼손·부하·주소 공간 원인을 배제하지 않는다.
  메모리 커밋량도 전체 주소 공간 점유와 다르다. 상세는 수집 안내의 진단 보강 항목을 참조한다.

## 12:36 KST — 공식 진단 미탐지, 진단 보강 수집 재개

- 사용자가 EasyGuide4ASTx 검사 결과 탐지 내역이 없다고 보고했다. 이는 기본 진단 항목 미탐지이며
  이전 충돌이나 장시간 안정성의 부정/인증이 아니다. 안랩 트레이 미표시 원인은 여전히 미확인이다.
- 기존 장애 검증·필요 시 재시작 허용 범위에서 진단 보강 코드로 새 raw-v2 수집기를 실행했다.
  사용자 로그인 후 mock 세션 `cf18cb437b9a4f6ba2abf0fdadbbfe57`, PID 7804/12888,
  실행 코드 30989e2. 12:36:34 체결 7,943 / 호가 22,443,
  accepted=committed=30,386, pending/dropped=0, error=null을 확인했다.
- 충돌/침묵 로그: `logs/collector_fault_20260917T033545979538Z_7804.log`.
  기동 로그: `operations_state/collector_launch_20260917_123544.*.log`.
  수신 재발 시 해당 진단과 상태/통신 기록을 대조한다. 모듈 업데이트는 확인하지 못했고 해결 완료가 아니다.

## 12:32 KST — 실제 재부팅 완료, 안랩 트레이 미표시 지속

- LastBootUpTime=12:27:55.5로 Windows 재부팅을 확인했다. 안랩 서비스와 StSess/StSess32는 새 PID로
  실행 중이나 사용자가 트레이 미표시 지속을 보고했다. Python 수집기는 실행하지 않았다.
- 공식 ASK 장애대응 페이지의 EasyGuide4ASTx.exe를 `operations_state/ahnlab_diagnostics/`에 받았다.
  2,762,128바이트, Authenticode Valid / AhnLab, Inc. 서명을 확인했다. 아직 실행하지 않았다.
- 이번 접속에는 Computer Use 도구가 있으나 해당 스킬은 보안/안티멀웨어 앱 UI 자동화를 금지한다.
  사용자에게 도구를 직접 실행하여 시작 버튼을 누르고 결과 코드/문구를 알려 달라고 안내한다.
  재부팅·재설치를 반복하거나 공유 VC++ 런타임을 추정으로 제거하지 않는다.

## 12:26 KST — 사용자 승인 Windows 재시작 준비

- 사용자가 에이전트에게 Windows 재시작을 직접 요청했다. 직전 Python 수집 프로세스 부재를 확인했다.
- 두 번째 수집 세션 e13522d545fc416188a2b882094772a8은 12:05:39 중복 로그인/재접속 이벤트로
  interrupted 종료됐으며 정상 closed로 인증하지 않는다. 원본은 보존한다.
- 안랩 트레이 미표시가 계속됐고, 실제 OS 마지막 부팅은 07:02:52, 안랩 서비스 시작은 07:03이었다.
  12:12 StSess 재기동은 Windows 재부팅이 아니었다. 이 기록 저장 후 강제 앱 종료 없는 OS 재시작을 요청한다.
  이 문구는 재부팅 완료 증거가 아니다. 재접속 후 LastBootUpTime과 안랩 트레이/업데이트 결과를 확인한다.
- 수집기는 자동 재실행하지 않는다. 안랩 상태 정리 후 진단 코드가 포함된 새 세션을 로그인해 검증한다.

## 최우선 작업 — 수신 정체/충돌 원인 제거

- 사용자는 오늘 수집 유지보다 장애 분석과 해결을 우선한다고 명시했고, 검증에 재시작이 필요하면
  알려 달라고 했다. 현재 새 수집기는 추가 재시작하지 않았다.
- Windows 덤프의 두 파서 결과 일치: c0000005, 실행 접근 위반 [8,0], 충돌 스레드 14636,
  EIP=0. 스택 상단이 AhnLab `mkd25sdk.dll+0x76fb`, 직전 간접 call의 대상 포인터는 덤프에서 0이다.
  DLL의 PE 타임스탬프/크기도 대조했다. **보안 모듈 내부 null 함수 호출을 강하게 지지**한다.
  전체 심볼 스택·선행 메모리 훼손 여부·10:31 수신 정체와 인과관계는 미확인이다.
- 10:31 이후에도 통신 주기 송신과 Qt 상태 갱신은 계속됐다. 저장 큐 적체 근거는 없었다.
  사용자가 **11:30 직접 종료하지 않았음**을 확인했다. 사용자 종료에 따른 충돌로 설명하지 않는다.
- 충돌 덤프/보고서/분석 스크립트는 `operations_state/session_observations/20260917_crash/`에 보존.
  상세 주소·해시·한계는 [수집 안내](docs/COLLECTION_RUNBOOK.md)에 있다. 외부 전송하지 않았다.
- 다음 실행용 faulthandler와 구간당 1회/최대 3회 침묵 스택 기록을 추가했다.
  실제 운영 통계 루프 연결·기존 종료·별도 프로세스의 실제 faulthandler 합성 **19개 통과(1.38초)**.
  외부 보안 모듈 문제를 고쳤다고 주장하지 않는다. 실행 중인 세션에는 미적용이다.
- 보안 모듈 자체 업데이트/재설치는 공식 공급사 확인이 필요하다. 임의 제거·무력화하지 않았다.
  10:31 정체 재현 시 Python 스택과 OCX/통신 상태를 확보해야 한다. 현재 덤프는 충돌 스레드 하나만 포함한다.
- 설치 ASTx는 1.18.1.1997(설치일 20260914), mkd25sdk.dll은 2.7.0.45다.
  공식 도움말에 트레이의 수동 업데이트가 안내돼 있으나 최신 여부·이 충돌 수정 포함 여부는 미확인이다.
  현재 도구에는 Windows 트레이를 조작할 callable native UI가 없으므로 사용자 수동 업데이트 결과가 필요하다.
  업데이트 후 새 진단 코드로 재시작·사용자 로그인·재관측하는 것이 다음 단계다. 아직 해결 완료가 아니다.
  진단 코드는 실제 32비트 별도 프로세스에서도 OCX 없이 enable/dump/disable 검증을 통과했다.

## 현재 상태 — 사용자 승인 후 11:36 수집 재개

- 사용자가 원격 OCX 로그인 가능 상태에서 재시작을 명시적으로 승인했다. 기존 Python 수집기 부재 확인 후
  11:35:24 새 raw-v2 실행, 11:36:14 사용자 로그인 완료, 11:36:16 3,753종목 등록 완료를 확인했다.
- 새 세션 `e13522d545fc416188a2b882094772a8`, mock, PID 8732/18484, 실행 코드 1229d98.
  기존 raw와 충돌 세션은 그대로 두고 새 세션 DB에 기록한다. 방향/venue unknown, 기본 15:35 종료 유지.
- 11:36:40 accepted=29,952 / committed=29,951 / pending=1에서
  **11:37:21 accepted=committed=93,671 / pending=0**으로 증가했다.
  체결 24,173 / 호가 69,498, 드롭 0, error=null, 프로세스 존재를 대조했다.
  짧은 관측 구간의 수신·저장 재개 확인이며 충돌 원인 해결/장시간 안정성 인증은 아니다.
- 관측 사본: `operations_state/session_observations/20260917_resumed/`.
  기동 stdout/stderr: `operations_state/collector_launch_20260917_113524.*.log`.
- 앞선 세션의 10:31 이후 결손과 비정상 종료 상태는 유지한다. 재시작으로 복원된 데이터가 아니다.
  추가 재시작이나 전체 raw 검사 없이 현 세션을 수집하도록 둔다.

## 이전 상태 (11:22 KST 읽기 전용 관측)

### 후속 정정 — 11:33 KST 충돌 확인

- 11:32:55 조회에서 PID 18796/18816 모두 부재. 상태 파일은 11:30:17 running에서 멈췄고
  closed/finalization 및 정상 종료 로그가 없다. 정상 저장 종료로 판정하지 않는다.
- Windows Application Error 1000: **11:30:26 python.exe / PID 0x496C(18796) / 예외 0xc0000005**,
  모듈 unknown. 11:30:30 WER 1001 BEX도 대조했다. 원문은
  `operations_state/session_observations/20260917_crash/`에 보존했다.
- 10:31 수신 정체와 11:30 프로세스 충돌은 서로 다른 시점이다. 공통 원인인지 미확인이다.
  Python/네이티브 구성요소 어느 쪽 결함인지 로그만으로 특정하지 않는다.
- e36fc83..현재 collector 차이는 운영에서 호출하지 않는 nxt_probe.py 추가뿐이다.
  이번 NXT 가드 변경의 직접 영향 근거는 없다. 어제 정규장 v1과 오늘 v2는 경로가 달라
  어제 성공만으로 오늘 raw-v2 장시간 안정성을 보증하지 못한다.
- 사용자는 재시작의 타당성을 문의했다. 이 작업에서 중단·재로그인·재실행하지 않았다.
  재개 시 기존 DB를 유지하고 새 세션으로 수신/저장 증가를 확인해야 하며 원인 해결과 구분한다.

- 세션 `1b120bb4b5874db8a6271c1c47edca77`, PID 18796(32비트 본체)/18816(실행기),
  07:26:39 시작을 상태 파일과 Get-Process로 대조했다. mock/raw-v2, 실행 코드 e36fc83이다.
  최신 상태 시각 11:22:10, running/accepting, writer_closed=false/finalization=null이다.
- **수신 정체:** 마지막 수신·저장 10:31:20. 11:16~11:22 두 관측 모두 체결 4,023,999,
  호가 7,236,363, accepted=committed=11,260,362, committed_seq=15,296,633으로 동일했다.
  로그에 50분 이상 신규 수신 없음 경고가 있다. queued/in_flight/pending/dropped=0, error=null이어도
  정상 실피드 수신이나 전체 무누락을 뜻하지 않는다. 원인은 미확인이다. 수집기·로그인·정책을 변경하지 않았다.
- 09:00~09:10 저장 진행 관측과 현재 정체를 구분한다. 3,753종목 구독 이력도 수신 완전성 인증이 아니다.
  후속 상태/로그 tail/프로세스 관측 사본은 `operations_state/session_observations/20260917_112429/`에 보존했다.
- 기존 예약 확인: `C:/Users/k10k2/.codex/automations/*/automation.toml`과 automation view에서
  09:00 COUNT=1 예약만 확인했다(파일 상태 ACTIVE와 추가 실행 예정은 같은 의미가 아니다).
  Windows 사용자 예약의 시각/동작에도 15:38 검증 예약은 없었다. 다른 호스트 예약 여부는 미확인이다.
  이 작업에서 새 예약을 만들지 않았다. 15:38 자동 검증이 확보됐다고 가정하지 않는다.

## 이번 변경과 검증

- 시작 작업 트리는 깨끗했고 HEAD는 이미 `464193d`였다. `2385928` 이후 들어온 거래소/coverage 분리를
  중복 구현하지 않고 구간 밖 입력만 있는 경우·알 수 없는 venue·빈 접미사 코드 반례를 추가 보강했다.
- 기본 미검증은 계속 차단한다. 명시적 허용 시에도 unverified/None을 유지하며 거래 0건의 결과에도
  manifest.params.nxt_guard에 정책·허용·판정·이유를 남긴다. confirmed는 호출자 선언이며 운영 인증이 아니다.
- signed_volume 구현은 변경하지 않았다. 무부호·0·비정상 문자열은 unknown을 유지한다.
  합성 저장에서 정상 +/-의 방향 오류 추가 레코드만 줄고 가격/시간 등 다른 오류와 연구 실패는 보존된다.
- NXT 오프라인 계획/관측 순수 함수와 원문 보존 테스트를 추가했다. 운영 수집기 연결·실제 로그인·
  구독 변경은 하지 않았다. 상세 계약과 실측 순서는 [수집 안내](docs/COLLECTION_RUNBOOK.md)에 있다.
- 관련 작은 합성 회귀 **77개 통과(2.65초)**. 운영 DB는 열지 않았고 raw/Daily_baseline/old_data는 보존했다.

## 다음 작업

1. 사용자가 외출 중이므로 현 수집기를 재시작·교체하거나 추가 로그인하지 않는다. 수신 정체 원인은 미확인으로 둔다.
2. 실제 종료 후 로그·동일 세션 상태·보고·OS 프로세스를 대조한다. 기본 15:35는 종료 완료 증거가 아니다.
3. 정상 종료·저장 완료 확인 뒤 수집 안내의 제한된 정규장 raw-v2 표본으로 FID 15/가격 부호/타입을 확인한다.
   다음 실행의 signed_volume 적용은 이 대조 뒤 결정한다. 오늘 정책과 기존 raw를 덮어쓰지 않는다.
4. 별도 소규모 NXT 실측을 준비한다. 15:40~20:00 애프터마켓은 현재 15:35 종료와 맞지 않는다.
   SetRealReg 접미사 지원·mock 지원·개별 체결 거래소·전체 구간 coverage는 아직 미실측이다.
5. 원격 사용은 기존 PC 원격 접속/localhost로 충분하다. 별도 인터넷 공개는 하지 않는다.

## 아래는 이전 관측 기록 (현재 상태는 위 항목 우선)

## 이어서 (2026-09-17)

- 후속 공식 명세 대조: 설치된 `C:/OpenAPI/koa_devguide.xml`에서 NXT/통합 종목코드 접미사,
  실시간 타입 단위 FID 수신, FID 15 부호의 매수/매도 의미를 확인했다. 근거 행과 실측 구분은 수집 안내에 있다.
  오늘 실행 중 수집기·구독·방향 정책은 변경하지 않았다. 저장한 FID 목록은 전체 수신 가능 필드 목록이 아니다.
- `2385928` 코드에서 기본 미확인 차단은 확인했다. 다만 `per_event_venue` + 빈 입력이 `calm/False`가 되는
  반례를 직접 재현했다. NXT 구간 수신 완전성 없이 미과열을 인증하는 경로는 후속 보강 대상이다.

- Cowork 세션에서 git 상태 → AGENTS.md → HANDOFF.md 순서로 확인했다. 추적 파일 124개가 통째로 바뀐 것처럼
  보였으나 `git diff --stat -w` 대조로 전부 CRLF/LF 줄바꿈 잡음뿐임을 확인했다(실제 내용 변경 0). 작업 트리를
  HEAD로 되돌리고 `.gitattributes`(`* text=auto eol=lf`, db/이미지/parquet 이진 제외, ps1/bat만 CRLF 유지)를
  추가해 재발을 막았다. 커밋 `5ba73f4`는 이 원격 브리지에 git push 자격 증명이 없어 origin에는 아직 반영되지
  않았다. 로컬 터미널이나 다음 작업에서 `git push`만 하면 된다.
- `operations_state/kiwoom_collector.lock`은 여전히 0(closed, writer_closed)이라 실행 중인 수집기·화면이
  없음을 재확인했다. 이번 세션은 새 프로세스를 시작하지 않았다.
- 남은 실환경 확인 5개 항목을 재검토했다: 1번(실피드 세션 검증)은 사용자 직접 OCX 로그인과 KRX 정규장
  (09:00~15:30 KST)이 동시에 필요해 이 세션 혼자서는 진행할 수 없다(확인 시점은 07시대 KST). 2번(KRX 가격
  `LOGOUT`)은 정식 공급자 인증 설정이라는 사용자/관리 영역 대기다. 3번(원격 접근 방식)은 사용자가 기존 PC
  원격 접속 사용을 이번에 재확인했으므로 추가 조치가 없다. 4번(재부팅/전원 손실 내구성)은 AGENTS 원칙상
  작업 PC를 임의로 재부팅하지 않으므로 사용자 승인 없이는 진행하지 않는다. 5번(활성 관리자 전체 이력의 압축
  checkpoint·원격 소켓 인증 서비스)은 아직 설계 자체가 없다 — 다음 설계 착수 후보로 남긴다.


## 다음 대화 시작점

- 사용자가 원격 사용 방식을 **기존 PC 원격 접속 안에서 사용**으로 확정했다. localhost를 유지한다.
  별도 IdP/도메인/TLS 구축은 현재 필수 작업이 아니다. 외부 공개 요청이 생길 때만 다시 연다.
- 1종목·60초 OCX 모의서버 실행은 종료했고 raw 저장 대조도 끝냈다. 추가 로그인 프로세스를 남기지 않았다.
  실피드 0건이므로 다음은 실제 수신 가능한 시간의 작은 세션 검증이다. 같은 장외 접속을 반복하지 않는다.
- KRX `LOGOUT`은 정식 인증 접근이 필요하다. 인증을 우회하거나 현재 shares를 과거 값으로 채우지 않는다.
- PC 재부팅/전원 손실은 미실측이다. 작업 PC를 임의로 재부팅하지 않는다. 프로세스 중단 테스트와 구분한다.

## 현재 구현

- 새 운영 수집기의 기본은 raw v2다. 원문 FID → 제한 큐 → 저장·drain·닫기 보고를 연결했고,
  방향/venue 미확인은 unknown으로 보존한다. 기존 raw v1/Daily_baseline/old_data는 수정하지 않았다.
- 화면이 만든 1~10종목·1~300초 세션만 시작/종료한다. 토큰·소유 프로세스·서버·요청/수락을
  SQLite에 보존한다. 미수락 취소는 뒤늦은 기동 권한을 폐기하고, 불확실한 stop을 재전송하지 않는다.
- `capture_health.py`: 관리 화면이 새 challenge를 발급하고 5초 heartbeat 순번/응답 및 OS identity를
  함께 확인한다. 관리자 재시작·PID 재사용·15초 무진행은 unknown이다. UTC 갱신·중복 응답은 생존을 연장하지 않는다.
  Qt 응답 확인이며 실피드 수신/데이터 품질 인증은 아니다. 제어 화면은 5초마다 갱신한다.
- 중단 후에는 수집 잠금·OS 종료를 확인하고 시작/drain/closed 저널을 대조한다. 종료 보고 부재는 failed다.
  소켓 제어 계약/관리자 이력은 별도 모듈이며, 운영 mailbox를 원격 소켓 서비스로 공개한 것은 아니다.
- 장외 재생 워커는 동일 수집 잠금 아래 32 MiB·10만 raw 이내의 manifest/전체 무결성을 확인한 뒤
  연구 엔진을 실행한다. 품질 오류는 실패로 남고 running 중단 작업은 자동 재실행하지 않는다.
- `history_maintenance.py`: 제어 DB 일관된 백업·quick_check·SHA256·동기화 후 checkpoint.
  기본 행 보존, 명시한 cutoff가 최소 30일 전일 때만 종료 행을 검증된 보관본으로 회전한다.
  raw/Daily/피어 저널은 대상이 아니며, 활성 실행·장중·64 MiB 초과는 차단한다.
- `replay_schedule.py`: 화면의 저장 계획을 Windows 현재 사용자로 1회 예약한다. 1분~7일 뒤 장외 시각,
  5분 만료, dispatch latch, 예약 취소/지난 예약 대조를 구현했다. 누락·중단의 자동 재실행은 없다.
  자격증명을 저장하거나 PC를 깨우지 않는다. 기존 OS 작업을 덮어쓰지 않고 수집/주문은 예약하지 않는다.
- `dashboard/access.py`: 기본 localhost. 외부 바인딩/강제 인증 시 OIDC·정확한 issuer/subject 허용 목록·
  exp를 검사한다. 모든 페이지와 fragment에 적용했다. IdP/TLS 설정 없으면 운영 기능을 차단한다.
  `.streamlit/secrets.toml`은 Git 제외다. 인증 의존성과 기존 pykrx의 setuptools 의존성을 uv.lock에 고정했다.
- 신규 기동/재생에 최소 256 MiB 디스크 여유 검사를 적용한다. 공간 예약이나 부하 인증은 아니다.
- 일봉 HTTP 성공과 요청 기간 가격 커버리지를 분리한다. 오래된 가격이 당일 메타데이터 축적을 막지 않는다.

## 이번 검증·실측

- 19:28:56~19:30:20 KST 실제 OCX 로그인(사용자 직접 입력)·삼성전자 1종목 구독·구독 후 60초 자동 종료.
  서버 응답은 **mock**, 체결/호가 콜백은 각각 **0건**. 프로세스 종료 코드 0은 저장 종료이며,
  세션 로그는 수신 0건 경고를 남겼다. 실피드 품질/부호/venue/로그인 공존은 인증하지 않았다.
  raw에는 session_start 1건만 있고 전체 읽기·체크섬·starting/draining/closed 보고·잔여 0을 대조했다.
  세션: `2818147a33f447e0a15fa35b411345d2`, 근거는
  `operations_state/capture_sessions/2818147a33f447e0a15fa35b411345d2/verification.json`.
- 실제 Streamlit 초기 기동에서 `dashboard/control_tower.py`가 최상위 패키지 이름을 가리는 오류를 발견해
  `dashboard/operations.py`로 변경했다. 모듈을 미리 import한 기존 테스트에서는 드러나지 않았다.
  새 인터프리터 실제 진입점·화면·인증 **21개 통과(10.94초)**. 실제 로컬 상태를 읽는 AppTest도 재검증 통과.
  임시 localhost 서버 health=`ok`, HTTP 200 확인 후 진단 서버를 종료했다. 원격 클라이언트 연결 자체는 미실측.
  전후 근거: `operations_state/dashboard_probes/6c077fb06a4a42fb8a1f1901b4a8350e/`.
- 19:07:56 KST 예약을 Windows가 정시 트리거했고 19:08:00까지 격리된 합성 호가 1건의 무결성 검사·
  실제 재생·결과 저장이 성공했다. 수동 `/Run` 없이 실행했고 새 인터프리터로 재호출해도 이력/결과는
  1개 그대로였다. 진단 예약은 삭제했다. 운영 raw·OCX는 사용하지 않았다.
  결과: `operations_state/scheduler_probes/022938435ab14d9b85b1c777170bc564/result.json`.
- 예약/장외 워커 테스트 **16개 통과(6.13초)**. 디스패치 직후와 워커 수락 직후 자식 프로세스를
  강제 종료하고 새 프로세스로 호출해도 queued/running 불확실 상태를 보존하며 재생하지 않는 반례 포함.
  실제 PC 재부팅/전원 손실 실측은 아니다.
- 진단 예약 삭제 시간 초과에도 결과 파일에 실행 오류·정리 오류·미삭제 상태를 남기는 테스트
  **1개 통과(0.77초)**. 정상 정시 진단에서는 해당 예약 삭제까지 성공했다.
- 추가 32비트 운영 수집기 `--preflight`: OCX 등록/파일과 raw-v2 기본 설정 확인.
  OCX 생성·로그인은 하지 않았다. 설치 준비 확인이며 실피드 정상성 판정은 아니다.
- 관련 합성 회귀 **357개 통과(30.13초)**. 실제 32비트 자식의 새 challenge 응답·raw 종료·관리자 복구 포함.
  인증 허용/거부·페이지 우회 차단·기한/취소/중복 예약·백업 후 회전을 작은 임시 데이터로 검사했다.
  최종 백업 동기화 실패·예약 취소·heartbeat 역행 반례를 포함한 **18개도 통과(1.89초)**했다.
- Windows Task Scheduler COM이 생성 XML을 수용했다. 18:40:55 KST에는 격리된 표식 전용 작업을
  실제 등록 → 수동 실행 → 표식 확인 → 해당 작업 삭제까지 성공했다. 운영 재생/OCX는 실행하지 않았다.
  결과: `operations_state/scheduler_probes/6b61f1e617ce4679a9f427eb48dab4ef/result.json`.
- 18:17 KST Python 수집기 부재를 관측했다. 오늘 장중 v1 저장 검사와 18:14 32비트 백엔드 합성
  기록 등 이전 실측은 [이전 인계](docs/archive/HANDOFF_20260916_PRE_HEARTBEAT.md)에 보존했다.
- 18:32 KST pykrx 1.0.51 비수정 가격/과거 shares 후보 진단: 종목 목록 2회 성공 후 가격 HTTP 400,
  응답 `LOGOUT`. 이후 요청은 중단했다. 실제가 공급자 인증·검증은 해소되지 않았고 Daily에 쓰지 않았다.
  `operations_state/price_source_probes/79fd6005c32e416cac0289035bb8d55d/result.json`에 응답을 보존했다.
- 같은 응답과 로컬 연구 유니버스 2,221개 대조: 상장 목록만 2,055, 상폐 목록만 166,
  양쪽/누락 0. `listing_reconciliation.json`에 근거·코드를 보존했다. 거래 가능 여부/보통주/PIT 인증은 아니다.
  과거 연구용 종목은 삭제하지 않았다. 상폐 2종목 공시 근거는 수집 안내에 있다.

## 남은 실환경 확인과 범위

1. 실피드가 있는 시간에 작은 OCX 세션으로 필드·부호·venue·로그인 공존·콜백 지연·저장 오류를 확인한다.
   지금의 장외/합성 성공을 이 검증으로 대체하지 않는다. 검증 전 전체 유니버스·장시간 관리 제한을 풀지 않는다.
2. KRX 가격 요청은 `LOGOUT` 상태다. 정식 공급자 접근 설정 뒤 실제가/분할일 경계·과거 shares를 검증한다.
   목록 대조만으로 D-6/PIT를 통과시키지 않는다. 해당 실패가 raw/당일 스냅샷 수집을 막지 않게 유지한다.
3. 기존 PC 원격 접속 사용으로 확정했다. localhost 화면 기동은 확인했고 별도 인터넷 공개는 하지 않는다.
   OIDC 왕복/IdP/TLS는 향후 외부 공개를 요청하는 경우의 선택 확장이다.
4. OS 정시 트리거와 합성 입력의 실제 재생·중복 차단은 확인했다. PC 재부팅/전원 손실 시나리오는 미실측이다.
   검증을 위해 PC를 재부팅하지 않았다. 기존 수집 자동 재기동·주문 기능은 별도 계약 범위다.
5. 운영 이력 유지 도구는 jobs/managed_captures에 적용한다. 소켓 관리자 전체 재생 이력의 압축,
   원격 소켓 인증 서비스와 대규모 부하는 별도 설계/실측이 필요하다.

상세 계약·사용법은 [CONTROL_TOWER §5-5](CONTROL_TOWER.md#5-5-상시-응답이력-유지예약접근-제어),
공급자/필드 확인은 [수집 안내](docs/COLLECTION_RUNBOOK.md), 연구 결과 의미는 [틱 연구 안내](TICK_RESEARCH_RUNBOOK.md)를 본다.
사용자는 커밋·푸시마다 보고를 원하지 않는다. 기능·검증·실환경 미확인 항목 중심으로 소통한다.
