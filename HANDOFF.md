# 인수인계 노트 (2026-09-15 작성 — 새 세션/컨텍스트로 넘어갈 때 먼저 읽을 것)

## 지금 상태
- ARCHITECTURE_V2.md 진행 로그 9차까지 반영 완료 (프로젝트 + 집컴 저장소 `C:\Projects\Stock` 양쪽 동기화됨).
- B-4(유니버스 단일 소스화), B-5(BLOB/UnboundLocalError), 발견 #9(수집기 무응답 오보고) 전부 코드 반영·검증 완료.
- 2026-09-15 오늘 낮: 절전 모드 설정 수정 + session_monitor.py 배포 후 첫 실전 수집 진행 중.
  **장 마감(15:30) 후 반드시 확인**: 마지막 수신 시각이 15:29~15:30 근처인지(절전 수정 검증),
  종료 로그가 `✅ 정상 종료`인지, `logs/kiwoom_universe_20260915.log`에 세션 요약·침묵 경고가 남았는지,
  종료 시 대기큐 잔량(발견 #13) 값.

## 대기 중 — 아직 실행 안 한 에이전트 작업 (프롬프트는 이미 작성해서 사용자에게 전달됨, 이 세션 대화에만 있음)

1. **build_features.py 실규모** (집컴 필수 — `sampledata/temp/20260914_LOB.db` 756MB, 3,634종목 필요)
   - `--limit 100` 스모크 → 외삽 보고 → 승인 후 전 종목
   - 커버리지 게이트가 거시 피처 0.08%(3/3,634)에서 **울리는 게 정상** — 안 울리면 버그
   - 1행짜리 5종목(407300/433250/438740/453060/498180) warmup 처리 확인
   - sampledata/Daily/*.csv, temp/20260914_LOB.db 건드리지 말 것

2. **daily_collector.py 유니버스 하드코딩 해제** (코드는 어디서든, 최종 재수집 실행은 집컴)
   - 3종목(000270/005930/000660) 하드코딩 → core/universe.py(B-4) 재사용
   - float_ratio 스텁(60.0 상수) 처리 결정 필요 — 실크롤링 구현 또는 피처 미출력
   - **검증 통과 조건**: 재생성 후 2022년 8일 회귀(거래 5건, 누적 -1.301%)가 그대로여야 함
   - 실행 전 sampledata/Daily/*.csv 백업 필수

두 작업 다 지난 대화에서 상세 프롬프트를 이미 작성해뒀음 — 새 세션에서 필요하면 이 노트를 근거로 다시 작성 가능.

## 환경 주의사항
- 체크아웃 2개: 학원(`C:\Projects\03_Personal_Quant\Stock`), 집(`C:\Projects\Stock`). **GitHub로만 동기화됨** — Cowork 세션은 한 번에 컴퓨터 1대만 연결되고, 컴퓨터를 바꾸면 이전 연결은 사라짐(추가되는 게 아니라 대체됨).
- 컴퓨터 옮기기 전 항상: 현재 컴퓨터에서 git commit + push → 도착 후 git pull.
- 폴더 연결은 세션마다 새로 필요할 수 있음(데스크톱 앱에서 폴더 재연결).
- 실데이터(raw_ticks/*.db, temp/*.db, features/*.parquet)는 .gitignore 대상이라 git으로 안 따라감 — 집컴에만 존재.

## 우선순위 (B-4/B-5 이후)
`A ✅ → B ✅ → B-1 ✅ → B-3 ✅ → B-4 ✅ → B-5 ✅ → [오늘 수집 검증] → 위 대기 작업 ①② → C(전략 추상화)`
B-2(fs_v2 포맷)는 여전히 보류 — §3.5.3 실측상 진짜 제약은 L0 raw 보존 정책(B-6)이지 L2 포맷이 아님.
