# 현재 인계 — 2026-09-16 / 정시 예약 재생·중단 후 중복 방지 실측

## 현재 구현

- 새 운영 수집기의 기본은 raw v2다. 원문 FID → 제한 큐 → 저장·drain·닫기 보고를 연결했고,
  방향/venue 미확인은 unknown으로 보존한다. 기존 raw v1/Daily_baseline/old_data는 수정하지 않았다.
- 화면이 만든 1~10종목·1~300초 세션만 시작/종료한다. 토큰·소유 프로세스·서버·요청/수락을
  SQLite에 보존한다. 미수락 취소는 뒤늦은 기동 권한을 폐기하고, 불확실한 stop을 재전송하지 않는다.
- `capture_health.py`: 관리 화면이 새 challenge를 발급하고 5초 heartbeat 순번/응답 및 OS identity를
  함께 확인한다. 관리자 재시작·PID 재사용·15초 무진행은 unknown이다. UTC 갱신·중복 응답은 생존을 연장하지 않는다.
  Qt 응답 확인이며 실피드 수신/데이터 품질 인증은 아니다. 제어 화면은 5초마다 갱신한다.
- 중단 후에는 수집 잠금·OS 종료를 확인하고 시작/drain/closed 저널을 대조한다. 종료 보고 부재는 failed다.
  소켓 제어 계약/관리자 이력은 별도 모듈이며, 운영 mailbox를 원격 소켓 서비스로 공개한 것은 아니다.
- 장외 재생 워커는 동일 수집 잠금 아래 32 MiB·10만 raw 이내의 manifest/전체 무결성을 확인한 뒤
  연구 엔진을 실행한다. 품질 오류는 실패로 남고 running 중단 작업은 자동 재실행하지 않는다.
- `history_maintenance.py`: 제어 DB 일관된 백업·quick_check·SHA256·동기화 후 checkpoint.
  기본 행 보존, 명시한 cutoff가 최소 30일 전일 때만 종료 행을 검증된 보관본으로 회전한다.
  raw/Daily/피어 저널은 대상이 아니며, 활성 실행·장중·64 MiB 초과는 차단한다.
- `replay_schedule.py`: 화면의 저장 계획을 Windows 현재 사용자로 1회 예약한다. 1분~7일 뒤 장외 시각,
  5분 만료, dispatch latch, 예약 취소/지난 예약 대조를 구현했다. 누락·중단의 자동 재실행은 없다.
  자격증명을 저장하거나 PC를 깨우지 않는다. 기존 OS 작업을 덮어쓰지 않고 수집/주문은 예약하지 않는다.
- `dashboard/access.py`: 기본 localhost. 외부 바인딩/강제 인증 시 OIDC·정확한 issuer/subject 허용 목록·
  exp를 검사한다. 모든 페이지와 fragment에 적용했다. IdP/TLS 설정 없으면 운영 기능을 차단한다.
  `.streamlit/secrets.toml`은 Git 제외다. 인증 의존성과 기존 pykrx의 setuptools 의존성을 uv.lock에 고정했다.
- 신규 기동/재생에 최소 256 MiB 디스크 여유 검사를 적용한다. 공간 예약이나 부하 인증은 아니다.
- 일봉 HTTP 성공과 요청 기간 가격 커버리지를 분리한다. 오래된 가격이 당일 메타데이터 축적을 막지 않는다.

## 이번 검증·실측

- 19:07:56 KST 예약을 Windows가 정시 트리거했고 19:08:00까지 격리된 합성 호가 1건의 무결성 검사·
  실제 재생·결과 저장이 성공했다. 수동 `/Run` 없이 실행했고 새 인터프리터로 재호출해도 이력/결과는
  1개 그대로였다. 진단 예약은 삭제했다. 운영 raw·OCX는 사용하지 않았다.
  결과: `operations_state/scheduler_probes/022938435ab14d9b85b1c777170bc564/result.json`.
- 예약/장외 워커 테스트 **16개 통과(6.13초)**. 디스패치 직후와 워커 수락 직후 자식 프로세스를
  강제 종료하고 새 프로세스로 호출해도 queued/running 불확실 상태를 보존하며 재생하지 않는 반례 포함.
  실제 PC 재부팅/전원 손실 실측은 아니다.
- 진단 예약 삭제 시간 초과에도 결과 파일에 실행 오류·정리 오류·미삭제 상태를 남기는 테스트
  **1개 통과(0.77초)**. 정상 정시 진단에서는 해당 예약 삭제까지 성공했다.
- 추가 32비트 운영 수집기 `--preflight`: OCX 등록/파일과 raw-v2 기본 설정 확인.
  OCX 생성·로그인은 하지 않았다. 설치 준비 확인이며 실피드 정상성 판정은 아니다.
- 관련 합성 회귀 **357개 통과(30.13초)**. 실제 32비트 자식의 새 challenge 응답·raw 종료·관리자 복구 포함.
  인증 허용/거부·페이지 우회 차단·기한/취소/중복 예약·백업 후 회전을 작은 임시 데이터로 검사했다.
  최종 백업 동기화 실패·예약 취소·heartbeat 역행 반례를 포함한 **18개도 통과(1.89초)**했다.
- Windows Task Scheduler COM이 생성 XML을 수용했다. 18:40:55 KST에는 격리된 표식 전용 작업을
  실제 등록 → 수동 실행 → 표식 확인 → 해당 작업 삭제까지 성공했다. 운영 재생/OCX는 실행하지 않았다.
  결과: `operations_state/scheduler_probes/6b61f1e617ce4679a9f427eb48dab4ef/result.json`.
- 18:17 KST Python 수집기 부재를 관측했다. 오늘 장중 v1 저장 검사와 18:14 32비트 백엔드 합성
  기록 등 이전 실측은 [이전 인계](docs/archive/HANDOFF_20260916_PRE_HEARTBEAT.md)에 보존했다.
- 18:32 KST pykrx 1.0.51 비수정 가격/과거 shares 후보 진단: 종목 목록 2회 성공 후 가격 HTTP 400,
  응답 `LOGOUT`. 이후 요청은 중단했다. 실제가 공급자 인증·검증은 해소되지 않았고 Daily에 쓰지 않았다.
  `operations_state/price_source_probes/79fd6005c32e416cac0289035bb8d55d/result.json`에 응답을 보존했다.
- 같은 응답과 로컬 연구 유니버스 2,221개 대조: 상장 목록만 2,055, 상폐 목록만 166,
  양쪽/누락 0. `listing_reconciliation.json`에 근거·코드를 보존했다. 거래 가능 여부/보통주/PIT 인증은 아니다.
  과거 연구용 종목은 삭제하지 않았다. 상폐 2종목 공시 근거는 수집 안내에 있다.

## 남은 실환경 확인과 범위

1. 실피드가 있는 시간에 작은 OCX 세션으로 필드·부호·venue·로그인 공존·콜백 지연·저장 오류를 확인한다.
   지금의 장외/합성 성공을 이 검증으로 대체하지 않는다. 검증 전 전체 유니버스·장시간 관리 제한을 풀지 않는다.
2. KRX 가격 요청은 `LOGOUT` 상태다. 정식 공급자 접근 설정 뒤 실제가/분할일 경계·과거 shares를 검증한다.
   목록 대조만으로 D-6/PIT를 통과시키지 않는다. 해당 실패가 raw/당일 스냅샷 수집을 막지 않게 유지한다.
3. 원격 접속 방식 질문에 아직 답이 없어 기존 PC 원격 접속을 기본으로 유지했다. 외부 연결에는
   사용자 도메인/TLS·IdP 등록·허용 운영자 설정이 필요하다. 실제 OIDC 왕복 인증/외부 공개는 미실측이다.
4. OS 정시 트리거와 합성 입력의 실제 재생·중복 차단은 확인했다. PC 재부팅/전원 손실 시나리오는 미실측이다.
   검증을 위해 PC를 재부팅하지 않았다. 기존 수집 자동 재기동·주문 기능은 별도 계약 범위다.
5. 운영 이력 유지 도구는 jobs/managed_captures에 적용한다. 소켓 관리자 전체 재생 이력의 압축,
   원격 소켓 인증 서비스와 대규모 부하는 별도 설계/실측이 필요하다.

상세 계약·사용법은 [CONTROL_TOWER §5-5](CONTROL_TOWER.md#5-5-상시-응답이력-유지예약접근-제어),
공급자/필드 확인은 [수집 안내](docs/COLLECTION_RUNBOOK.md), 연구 결과 의미는 [틱 연구 안내](TICK_RESEARCH_RUNBOOK.md)를 본다.
사용자는 커밋·푸시마다 보고를 원하지 않는다. 기능·검증·실환경 미확인 항목 중심으로 소통한다.
