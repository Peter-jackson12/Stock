# 현재 인계 — 2026-09-16 / 운영 진입점 raw v2·상태 화면 연결

## 이번 구현과 검증

- 사용자 운영 적용 요청에 따라 `kiwoom_universe_logger.py` 기본 저장을 raw v2로 전환했다.
  다음 새 실행부터 적용되며, `--storage raw-v1`은 명시적인 호환 옵션이다. 기존 파일은 보존한다.
- `live_capture.py`: 원문 FID·진입 단조/UTC 시각 → 제한 큐 → 실제 저장/닫기 보고·피어 저널 연결.
  방향/venue는 unknown이며 FID 14 방향 추정을 새 경로에 사용하지 않는다. 콜백/등록/연결 오류는 중단한다.
  CLI 중복 기동을 OCX 생성 전에 잠금으로 차단한다. 로컬 종료를 원격 stop 수락으로 위장하지 않는다.
- 상태 파일을 약 5초마다 기록하고 운영 화면에 접수 콜백·커밋 raw·미커밋 콜백을 분리 표시한다.
  상태 파일은 관측 자료이며 제어 heartbeat/생존 인증은 아니다. 화면 제어·운영 IPC는 아직 미연결이다.
- 실제 `.venv32` 사전 점검에서 32비트/PyQt·OCX 등록 파일 확인 성공. OCX 객체/로그인은 생성하지 않았다.
  17:34:36 KST 운영 백엔드 합성 점검: 콜백 20건 → session_start/방향 미확인 표식 포함 raw 41건,
  저장·종료·체크섬 재읽기 성공. 결과: `operations_state/live_backend_probes/1628cbce5af34257a68e56f026fc3464/result.json`.
  실피드 수신과 전체 유니버스 부하·지연 검증을 완료한 것은 아니다.
- `history_limits.py`: 피어 저장 기본 100,000보고/UTF-8 payload 128 MiB,
  관리자 복구 기본 100,000이벤트/128 MiB/반복 2초 제한을 추가했다.
  저장 한도 초과는 송신하지 않고 기존 보고를 보존한다. 복구 한도 초과는 소유권을 바꾸지 않는다.
- 피어 저널 v2는 정책·사용량을 원자적으로 저장한다. v1 전환도 건수·크기를 제한하고 실패 시 원상 보존한다.
  실제 파일 크기/WAL 제한·OS I/O 강제 기한·자동 삭제/회전/checkpoint를 구현한 것은 아니다.
- `control_tower/report_journal.py`: 보고를 송신 전에 별도 SQLite에 저장하고, 동일 revision 변경과
  순번 누락을 거부한다. 실패한 송신의 보고도 보존한다. lifecycle 의미 검증은 관리자 reducer가 수행한다.
- `CaptureConnection.reconcile_page()`: 전체 identity/cursor를 검증하는 최대 128개·32 KiB 페이지 대조.
  과거/빈 보고는 생존을 갱신하지 않는다. 중간 단절·잘못된 후속 상태는 페이지를 부분 반영하지 않는다.
  실제 32/64비트 자식이 작은 raw를 닫은 후 복구한 관리자가 새 소켓으로 drain/closed를 가져오고
  실제 manifest와 대조했다. 종료 명령을 재전송하지 않는다. 운영 재접속 서비스/소켓 인증은 미연결이다.
- `control_tower/windows_process.py`: 조회 전용 핸들로 PID·생성 시각·실행 파일·32/64비트를 대조한다.
  IPC 연결/stop 전송에 연결했다. 종료된 프로세스의 버퍼 보고는 읽되 생존으로 취급하지 않는다.
  Python 3.10의 UTC 형식 호환 문제를 실측으로 발견해 수정했다.
- `collector/kiwoom/queue_control.py`: 모든 콜백의 정규화·커밋 뒤 raw 순번으로 draining을 보고하고,
  파일 닫기 성공 뒤 실제 manifest로 closed를 보고한다. 단계별 마감 대기는 최대 5초이며 중단 시 incomplete다.
  수집 중 제어 heartbeat는 미구현이고, 콜백 backlog는 별도 상태 파일로 관측한다.
- `control_tower/ipc.py`: 최대 32 KiB 프레임과 기본 1초/최대 5초 전송·수신 제한.
  부분 읽기가 마감 시간을 연장하지 않으며 오류/단절 시 연결을 닫는다.
  `CaptureConnection`은 기존 내구성 이력·소유권 검사에 연결하고 전송 성공을 종료 완료로 취급하지 않는다.
- 실제 32/64비트 별도 Python 프로세스에서 부모/자식 OS 식별 대조와 작은 합성 raw 저장 → 종료 보고 →
  체크섬 재읽기를 확인했다. 파싱 오류 콜백 1건은 session_start 포함 raw 3건이며 품질은 unverified다.
  응답 없는 정상 exit, OS 종료 후 버퍼 보고, 전송 직전 종료, 부분 메시지·시간 초과·저장/닫기 실패도 검사했다.
- `collector/kiwoom/queued_capture.py`: 크기가 제한된 단일 입력 큐와 전용 SQLite 워커.
  입력 복사, 배치 커밋, 종료 drain, overflow/콜백 오류/저장 실패 보존을 구현했다.
  큐 초과·입력 오류는 incomplete로 남기며 정상 종료로 승격하지 않는다.
- `control_tower/capture_history.py`: 누락 보고 최대 128개의 원자적 대조와 전송 시도 선저장.
  과거 보고를 생존 증거로 쓰지 않고, 실제 전송 직전 소유권을 재검사한다.
  가짜 동기식 sender 수락과 관리자 소유권 교체를 직렬화한다. 불확실한 stop은 재전송하지 않는다.
- 관련 합성 테스트 **281개 통과(21.32초)**: 운영 콜백/종료/상태 화면, 이력 예산/스키마 전환, 피어 저널/재접속, OS 식별/IPC/종료 보고, 제어/복구/전송/누락 보고, 큐/세션/raw v2/정규화,
  기존 작업 관리. 실제 스레드·작은 임시 SQLite를 사용했고 OCX는 사용하지 않았다.
- `scripts/probe_raw_v2_queue.py`: 실제 32비트(16:30 KST)·64비트(16:34 KST) Python에서 각각
  합성 콜백 500건 → session_start 포함 501레코드 저장·닫기·체크섬 재읽기 성공.
  진단은 `operations_state/queue_probes/b0332afb1de8495283a992256a95e64b/result.json`(32비트),
  `operations_state/queue_probes/19d652a27020464daf3ad85d29ed82f7/result.json`(64비트)에 있다.
  소규모 연결 점검이며 실제 수집 부하/콜백 지연 인증이 아니다.
- 사용자 최신 결정: 검증된 변경의 커밋·푸시를 에이전트가 수행한다. 사용자 전용 푸시 지침을 제거했다.
  커밋·푸시 여부를 매번 별도로 보고하지 않고 기능·검증 결과 중심으로 소통한다.
  지침은 한국어이며, 범위 내 구현 방법·순서는 자율적으로 정한다.

## 운영 상태와 확인된 실측

- 16:15 KST에 수집 Python 부재·15:35 종료 로그·DB 마지막 시각 15:32:56을 대조했다.
  오늘 실행분에는 최신 종료 패치의 미커밋/저장 완료 표식이 없었다.
- 오늘 raw v1 7,046,569,984바이트를 16:18:41부터 167.494초간 읽기 전용 검사했다.
  `quick_check=ok`, 실제 체결 **12,989,488건**·호가 **29,704,393건**으로 로그와 일치,
  DB/WAL 변경 없음. 결과는 `operations_state/storage_checks/20260916_after_close.json`이다.
- fchart 표본 50종목은 HTTP/파싱 50/50 성공·재시도 0. 당일 날짜는 48종목이며 2종목은 오래된 응답이다.
  상세 측정과 기존 저장 검사 테스트 31개 기록은 [수집 안내](docs/COLLECTION_RUNBOOK.md) 및 Git 이력을 본다.
- 17:26 KST에도 실행 중인 Python 수집기 부재를 확인했다. 오늘 장중 실행분은 raw v1이고,
  다음 실행의 기본은 raw v2다. 운영 raw·Daily_baseline·old_data를 수정하지 않았다.
  추가 로그인/실피드 수집기 기동은 하지 않았다. 운영 IPC는 합성 자식에만 연결했으며 상태 화면만 운영 경로와 연결했다.

## 다음 작업

1. 수집 중 제어 heartbeat 계약과 관리 대상 시작·독점 채널 전달 어댑터를 연결한다.
   현재 종료 어댑터는 drain 전 raw 수를 추정하지 않는다. 관리자 checkpoint와 이력 보관/회전·디스크 여유 검사도 운영 전 필요하다.
2. `--codes 005930 --duration-seconds 60` 등 작은 실제 세션으로 OCX 필드·부호·venue·서버 공존,
   실제 콜백 지연·drain/저장 오류/재기동을 실측한다. 장외 무체결 실행은 실피드 검증이 아니다.
   현재 합성 검증이나 오늘 v1 저장 검사로 이 검증을 대체하지 않는다.
3. 위 검증 후 화면의 수집 시작/종료 → 종료 데이터 검사 → 장외 틱 재생 실행을 연결한다.
4. fchart 오래된 응답 2종목의 유니버스/거래 상태, 실제가 출처(D-6), 메타데이터·shares 백필은
   각 작업의 선행 조건으로 검증한다. 한 소스의 실패가 당일 스냅샷/raw 축적을 막지 않게 한다.

상세 계약은 [CONTROL_TOWER §5](CONTROL_TOWER.md#5-제어-계약과-후속-운영-연결),
과거 rev.2 근거·관측 이력은 [보관본](docs/archive/HANDOFF_20260916.md)에 있다.
