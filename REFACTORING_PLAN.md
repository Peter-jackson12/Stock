# 📌 주식 백테스팅 시스템 패키지화 & 리팩토링 계획

## 1. 개요 및 리팩토링 목적
- 현재 단일 파일(`BackTest.py`)에 집중된 로직을 기능별 모듈로 분리.
- 하드코딩된 로컬 경로를 제거하고, 누구나 한 줄로 실행 가능한 패키지 구조로 개편.
- `uv` 기반의 최신 파이썬 의존성 관리 및 가상환경 동기화 적용.

---

## 2. 기존 코드 문제점 분석 (Refactoring Checklist)
- [ ] **경로 문제:** `C:\Users\...` 로컬 절대 경로 사용 ➔ 프로젝트 상대 경로로 변경 필요
- [ ] **모듈화 부재:** DB 연결, 캔들 생성, 전략, 손절/익절이 하나의 클래스에 집약됨
- [ ] **재현 불가능:** 데이터가 없어 외부에서 실행 불가 ➔ 1일치 샘플 DB 포함 필요

---

## 3. 목표 모듈 구조 설계 (Target Architecture)
- `config.py`: 경로, 백테스팅 날짜, 전략 매개변수 통합 관리
- `data_loader.py`: SQLite DB 및 CSV 데이터 로드 전담
- `candle_generator.py`: 초봉/분봉 캔들 생성 및 업데이트 로직
- `strategy.py`: 진입 조건, 지표 계산, 호가 잔량 변화량 로직
- `risk_manager.py`: 손절/익절/트레일링 스탑 제어
- `engine.py`: 백테스팅 전체 루프 실행 및 결과 집계
- `main.py`: 실행 진입점 (1-Click 실행)

---

## 4. 실행 방법 목표
```bash
# 1. 의존성 동기화
uv sync

# 2. 백테스팅 원클릭 실행 (1일치 샘플 데이터 기반)
uv run python main.py
```
```
