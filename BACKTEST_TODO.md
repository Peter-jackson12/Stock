# 첫 실데이터 시험 백테스트 체크리스트

기준일: 2026-09-20. 목표는 **검증 가능한 실제 입력 하나로 원본 틱 → 신호 → 주문 → 체결 → 결과를 대조**하는 것이다.
전략 수익성 입증과 실거래 운영 승인은 별도다. 원본 틱 재생이 주 경로이며 LOB/초봉 변환은 선행 조건이 아니다.

이 문서는 현재 할 일과 후보 판정만 유지한다. 실행·결과 계약은 [틱 연구 런북](TICK_RESEARCH_RUNBOOK.md),
저장 종료는 [수집 런북](docs/COLLECTION_RUNBOOK.md), 작업 인계는 [HANDOFF](HANDOFF.md)를 따른다.
과거 파일 5개의 상세 조사와 의사결정 원문은 [보존본 안내](docs/archive/README.md)에 있다.

## 1. 이미 준비된 것

- [x] 닫힌 raw v2 읽기·순번/체크섬 검증·종목/venue 하나의 연구 실행과 JSON 저장.
  [연구 코드](engine/tick_research_run.py), [CLI](scripts/run_tick_research.py), [합성 회귀](tests/test_raw_v2.py).
- [x] 실패·미청산·체결 없음의 구분과 완료 근거 조회.
  [조회 코드](scripts/inspect_tick_research.py), [결과 계약](TICK_RESEARCH_RUNBOOK.md#결과-읽기).
- [x] 9월 18일 세션의 기존 종료 근거와 시작/중간/말미 총 300 records 표본 대조.
  정규화 비교 대상 291 ticks의 재계산 불일치 0건이나, 말미 방향 미확인 8 trades와 대응 parse_error 8건이 있다.
  근거: `f64c37c`, `operations_state/raw_sample_checks/a25fe6721db94d8cb2724ee2798a8ad7/`.
  이 표본을 이유 없이 반복하지 않는다. 전체 무결성·무누락·연구 합격은 미확인이다.
- [x] 품질 문제 처리 방향: 새 clean closed 세션을 기본 경로 A로 선택. 기존 raw 파생 경로 B는 예비.
  근거: 2026-09-20 결정 및 [정리 전 기록](docs/archive/README.md).

## 2. 가장 먼저 할 일 — 시험 입력 결정

**현재 바로 사용할 수 있다고 판정한 실제 raw 후보는 없다.** 다음은 기존 보고에 근거한 판정이며,
이번 문서 정리에서 raw를 새로 읽거나 전체 파일을 검사한 결과가 아니다.

| 기존 session_id | 확인된 제한 | 현재 처리 |
|---|---|---|
| `cf18cb437b9a4f6ba2abf0fdadbbfe57` (09-17) | 저장 당시 direction_policy=unknown. 같은 세션의 제한 조사 10,384건 중 구 방향 판정과 FID15 기반 방향이 891건(8.6%) 불일치 | 첫 기본 입력에서 제외. 최초 100건 검사를 다음 필수 작업으로 재등록하지 않음 |
| `21f8c124e64e421893275ccdc83818ad` (09-18) | signed_volume 이후 수집됐지만 기존 말미 표본의 방향 미확인 8 trades + 대응 품질 기록 8건 존재 | 현행 엄격 whole-file 연구 경로에서 차단. 다른 종목 선택으로 우회하지 않음 |
| 나머지 과거 3개 | 기존 근거상 사실상 빈 파일 또는 정상 종료 미확인 | 후보에서 제외. 세부 session/상태는 보존본 유지 |

8.6%는 해당 제한 조사에만 해당하며 전체 파일 오류율·성과 영향률이 아니다.
8 trades와 대응 parse_error를 16개의 독립 체결 오류로 세지 않는다. 표본 밖 문제가 없다고 단정하지 않는다.
`inspect_raw_v2_sample.py`는 저장 당시 정책을 재적용하므로 unknown 파일이 sample_consistent여도
현재 signed_volume 연구 적합성을 인증하지 않는다.

### 기본 경로 A — 새 clean closed 세션

- [ ] **수집 승인·당일 사전 점검:** 최신 코드·환경·서버·시장 공지·잠금·프로세스·공간 확인.
  수집 자체는 별도 승인 후 로컬에서 수행한다. 계획만으로 로그인·예약을 승인하지 않는다.
- [ ] **신규 세션 수집과 종료 근거:** 새 session_id·코드 revision·정책·피드 범위·원본 경로를 남긴다.
  같은 세션의 입력 중단·drain·writer_closed·finalization·오류/드롭·콜백/순번·보고/저널·로그·OS 프로세스 부재·파일 메타데이터를 대조한다.
  상세 종료 계약은 수집 런북을 따른다. closed 표시만으로 후보를 승인하지 않는다.
- [ ] **후보 지정:** 종료 근거가 맞는 신규 세션을 첫 시험 후보로 기록한다. 아직 연구 입력 합격은 아니다.
- [ ] **제한 표본:** [연구 런북의 최초 100건](TICK_RESEARCH_RUNBOOK.md#닫힌-raw의-제한된-표본-대조)부터 시작한다.
  이상 없으면 명시적인 중간/말미 제한 구간을 추가한다. 전체 COUNT/OFFSET/스캔으로 위치를 고르지 않는다.
  품질 오류나 불일치가 있으면 확대 재생을 멈춘다. 표본 일치가 전체 검사를 대신하지 않는다.
- [ ] **시험 범위:** 종목 하나·venue·청산 규칙 하나·독립 계좌 하나·수량·비용·지연·호가 나이를 명시한다.
  venue=unknown을 NXT 인증으로 바꾸지 않는다. 실행 가능성과 진입에 충분한 입력인지 구분한다.

### 예비 경로 B — 현재 구현하지 않음

새 clean 세션 확보가 계속 불가능하다는 근거가 생길 때만 재검토한다. 최신 날짜·크기만으로 기반 raw를 고르지 않는다.
채택 시 원본을 수정하지 않고 별도 dataset identity, 원본 session_id/seq 추적, 사용·제외 범위와 사유,
원문 역추적, 정규화 코드 hash와 정책, 전략 선행 구간·시가, 원본과 파생 결과의 재현 관계를 남겨야 한다.
오류 기록을 조용히 삭제하거나 무부호 값을 임의의 방향으로 채우는 것은 이 경로가 아니다.

## 3. 장외 실행 전 준비

- [ ] **실행 계획:** 입력·장외 시간·허용 I/O/시간·중단 기준·새 출력 경로·여유 공간을 확정한다.
  수집과 겹치지 않게 한다. 종목 하나 선택도 원본 전체 읽기를 줄이지 않는다.
- [ ] **검사 경로:** 화면의 32 MiB·100,000 raw 상한을 확인한다. 초과 파일에는 별도 대용량 검사/실행 계획이 필요하다.
  **현재 직접 연구 CLI는 검사 전용이 아니다.** 전체 읽기를 완료하며 순번·체크섬을 확정하는 과정에서
  전략 재생도 진행한다. 화면 워커의 선행 무결성 스캔과 구분한다.
  첫 시험에서 요구하는 "전략 실행 전 전체 입력 합격"을 충족할 검사 전용 절차를 먼저 확정·검증한다.
  도구가 필요하면 기존 reader/품질 계약을 재사용하며, 없는 `qualify` 명령을 실행하지 않는다.
- [ ] **입력 전체 검증:** 승인된 장외 범위에서 전체 순번·checksum·품질 제어 기록·whole-file 연구 입력 계약을 검사하고 근거를 보존한다.
  중간 품질 오류로 iterator가 끊겼다면 전체 checksum 완료로 보고하지 않는다.
  실패하면 입력 합격으로 처리하지 않는다. 기존 표본·closed·CI로 대체하지 않는다.

## 4. 첫 시험 실행과 결과 대조

- [ ] **시험 실행 1회:** 합격 입력·설정·코드 버전·입력 출처·결과 경로를 기록한다.
- [ ] **결과 상태:** 전체 입력 처리·오류·체결 없음·미청산을 구분한다. failed 중간 결과는 진단 전용이다.
- [ ] **대표 사례 대조:** 원본 이벤트 → 신호 → 주문 → 체결 가격/수량을 연결한다.
  진입/체결 사례가 없으면 원인을 남기고 해당 경로의 실데이터 검증은 미완료로 둔다.
- [ ] **재현성:** 같은 입력·설정·코드의 재실행에서 주문·체결·경제적 결과를 대조한다.
  실행 ID·생성 시각 등 달라져야 할 메타데이터는 분리한다. 재실행도 장외 I/O 계획에 포함한다.
- [ ] **종료 기록:** 확인 범위·성공/실패 이유·한계를 HANDOFF에 기록한다. 전략 수익성으로 확대하지 않는다.

## 5. 첫 시험 이후 — 전략 평가

- [ ] 여러 날짜·시장 상황·종목에서 품질 확인된 입력과 진입 사례를 축적한다.
- [ ] 학습/조정 구간과 평가 구간을 분리하고 비용·지연·체결 가정에 따른 민감도를 비교한다.
- [ ] 거래 수·손실 구간·미체결·미청산·데이터 제외 내역까지 검토한다. 용량만으로 데이터 충분성을 선언하지 않는다.

## 별도 진행 — 첫 시험의 필수 선행 조건이 아닌 것

- [x] 작은 합성 raw 압축·복원 시제품과 바이트 검증: `97e3777`, `6e1029f`, [회귀](tests/test_raw_archive.py).
- [ ] 실제 대용량 압축·복원의 용량/속도/내구성 검증. 원본 자동 삭제는 범위 밖이다.
- [ ] 실피드 장시간 부하·장애 복구·OCX/NXT 전환의 운영 실측. 연구에 필요한 원천 확인은 별도로 충족한다.
- [ ] 실거래 주문 연결과 운영 승인. 첫 시험 완료와 별개다.

운영 raw·Daily_baseline·old_data·사용자 변경·오류 근거를 보존한다.
체크 시 확인 날짜와 근거를 남기고, 상세 계약을 다른 문서에서 복제해 늘리지 않는다.
