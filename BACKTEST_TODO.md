# 첫 실데이터 시험 백테스트 체크리스트

기준일: 2026-09-19. 목표는 **검증 가능한 실제 입력 하나로 원본 틱 → 신호 → 주문 → 체결 → 결과를 대조**하는 것이다.
전략 수익성 입증과 실거래 운영 승인은 별도 단계다. 원본 틱 재생이 주 경로이며 LOB/초봉 변환은 필요하지 않다.

현재 위치: 합성 실행 경로는 준비됐지만 **첫 시험에 사용할 실데이터 입력은 아직 확정하지 못했다.**
9월 18일에 확인한 세션은 방향 미확인 체결 8건과 대응 parse_error 때문에 현재 엄격한 연구 경로에서 차단된다.
데이터를 더 모으는 것만으로 이 차단이 해결되지는 않는다.

이 문서는 현재 할 일의 목록이다. 실행 방법·판정 계약은 [틱 연구 안내](TICK_RESEARCH_RUNBOOK.md),
저장 종료 확인은 [수집 안내](docs/COLLECTION_RUNBOOK.md), 작업 이력은 [HANDOFF](HANDOFF.md)를 따른다.
체크할 때 확인 날짜·근거 경로 또는 커밋을 함께 적는다. 합성 통과를 실데이터 완료로 체크하지 않는다.

## 1. 이미 준비된 것

- [x] 닫힌 raw v2 읽기, 순번·체크섬 검증, 종목/venue 하나의 연구 실행과 JSON 저장 경로 구현.
  근거: [연구 실행 코드](engine/tick_research_run.py), [CLI](scripts/run_tick_research.py),
  [raw 회귀](tests/test_raw_v2.py). 합성 검증이며 실제 하루 재현 인증은 아니다.
- [x] 실패·미청산·체결 없음 등 결과 상태와 완료 근거 검사 구현.
  근거: [결과 조회 코드](scripts/inspect_tick_research.py), [결과 읽기 계약](TICK_RESEARCH_RUNBOOK.md#결과-읽기).
- [x] 9월 18일 세션 `21f8c124e64e421893275ccdc83818ad`의 저장 종료 근거와 시작·중간·말미 300건 표본 대조.
  틱 291건의 정규화 재계산 불일치 0건, 말미 방향 미확인 체결 8건·대응 parse_error 8건 확인.
  근거: `operations_state/raw_sample_checks/a25fe6721db94d8cb2724ee2798a8ad7/`, 커밋 `f64c37c`.
  표본을 반복 검사할 필요는 없다. 전체 무결성·무누락·연구 합격은 미확인이다.

## 2. 가장 먼저 할 일 — 시험 입력 결정

### 2026-09-20 후보 조사 — 현재 근거만으로 시험 후보 선정 불가

기존 저장 종료 근거·표본 검사 근거·`sampledata/raw_ticks_v2/` 파일 메타데이터만 대조했다.
raw DB를 새로 열거나 신규 표본 검사·전체 검사는 하지 않았다.

`sampledata/raw_ticks_v2/`의 실제 raw v2 파일 5개(경로·크기·`LastWriteTimeUtc`는 이번에 직접
`Get-ChildItem`으로 확인, 나머지는 `operations_state/capture_sessions/*/status.json`·
`operations_state/raw_sample_checks/…`·HANDOFF 기존 기록에서 확인):

| session_id | 날짜 | 수집 구간(KST, 기존 근거) | 파일 크기 | 종료 상태(기존 근거) | 알려진 품질 문제 |
|---|---|---|---|---|---|
| `2818147a33f447e0a15fa35b411345d2` | 2026-09-16 | 19:28~19:29, "제한 수집 시간 종료" | 12,288B | closed, finalization 있음 | event_count=1 — 연구에 쓸 수 없는 사실상 빈 파일 |
| `1b120bb4b5874db8a6271c1c47edca77` | 2026-09-17 | 07:27~10:31(수신 정체 후 11:30 프로세스 크래시) | 15,415,635,968B | **closed 아님** — writer_closed=false, finalization=null (Windows 예외 0xc0000005로 비정상 종료) | 종료 자체가 안 됨 |
| `e13522d545fc416188a2b882094772a8` | 2026-09-17 | 11:35~12:05, 중복 로그인/재접속으로 interrupted | 3,298,263,040B | state=interrupted, writer_closed=true지만 finalization=null | 정상 종료 보고 없음 |
| `cf18cb437b9a4f6ba2abf0fdadbbfe57` | 2026-09-17 | 12:35~15:32, "장 마감 (15:35)" | 21,477,777,408B | closed, writer_closed=true, finalization 있음 | 수집 코드 리비전 `30989e2c`(2026-09-17 12:33 KST)로, 같은 날 15:47 KST 커밋 `98d9394`(새 세션 기본 방향을 signed_volume으로 전환)보다 **이전**이다. 이 세션 자체의 10,384건 표본 대조(HANDOFF 15:38 KST 기록)에서 구세대 방향 판정이 스펙 대비 8.6% 불일치, 방향 정책 전환 시 제어 레코드 99.8%가 사라진다고 기록돼 있다 — 즉 저장된 방향 라벨이 대규모로 신뢰할 수 없다는 기존 근거가 이미 있다. 이 파일에 대한 `inspect_raw_v2_sample.py` 표본 대조는 아직 없다. |
| `21f8c124e64e421893275ccdc83818ad` | 2026-09-18 | 07:55~15:32, "장 마감 (15:35)" | 51,394,355,200B | closed, writer_closed=true, finalization 있음 | 수집 코드 리비전 `f7f4e7c5`(2026-09-18 07:53 KST, 방향 정책 전환 이후)로 시작/중간 표본 291틱 재계산 불일치 0건이지만, 말미(15:32:19~15:32:50 KST) 무부호 FID15 체결 8건과 대응 parse_error 8건이 있어 **현재 엄격 연구 경로가 거부한다.** |

**결론: 현재 근거만으로 시험 후보를 지정할 수 없다.**
저장이 정상 종료된 실제 raw v2 세션은 `cf18cb43…`와 `21f8c124…` 둘뿐이고(`2818147a…`는
사실상 빈 파일, `1b120bb4…`는 종료 자체가 안 됐고, `e13522d5…`는 종료 보고가 없다),
그 둘 다 이미 알려진 이유로 연구 입력으로 바로 쓸 수 없다.

- `21f8c124…`는 기존 표본 검사에서 확인된 말미 방향 미확인 8건·parse_error 8건 때문에
  선택 종목과 무관하게 현재 연구 경로에서 거부된다(§5, 기존 결정 유지, 이번에 재검토하지 않음).
- `cf18cb43…`는 방향 정책 전환 커밋(`98d9394`, 2026-09-17 15:47:59 KST) **이전** 코드로
  수집됐고, 같은 날 15:38 KST 기록된 10,384건 표본 대조에서 구세대 방향 판정 8.6% 불일치와
  방향 정책 적용 시 제어 레코드 99.8% 소멸이 이미 문서화돼 있다. 이는 "아직 확인 안 됨"이
  아니라 **이미 존재가 확인된 대규모 품질 문제**이며, `inspect_raw_v2_sample.py`로 실제
  저장값 대 현재 정규화기 재계산 불일치를 대조하기 전에는 연구 입력 후보로 올릴 근거가 없다.
  "최근 파일"·"큰 파일"·"날짜가 정상으로 보임"만으로 이 파일을 후보로 선정하지 않는다.

부족한 근거(파일·필드 단위):

- `cf18cb43…`에 대한 `inspect_raw_v2_sample.py` 표본(첫 100건 우선)의 `mismatches`/`quality_issues` —
  현재 없음. 있어야 이 파일의 방향/parse_error 문제가 21f8c124처럼 국소적인지 전면적인지 판단 가능하다.
- 위 표의 종료 상태·크기 이외 항목(정확한 event_count/manifest 전체 필드, 구독 종목 목록)은
  세 세션 모두 `status.json` 수준까지만 있고 21f8c124 수준의 `closure_evidence.json`류
  통합 기록은 `cf18cb43…`에는 없다.
- 종목/venue는 두 세션 모두 `kiwoom_universe_venue_unverified`(전 종목 구독, 거래소 미확정)로
  파일 자체에 고정된 단일 종목이 없다 — 연구 실행 시점에 임의 종목 선택이 필요하며 이번엔 정하지 않았다.

다음 최소 관측(이번에는 실행하지 않음, 계획만):

- 대상: `sampledata/raw_ticks_v2/20260917/cf18cb437b9a4f6ba2abf0fdadbbfe57.db`.
- 범위: `scripts/inspect_raw_v2_sample.py --start-seq 1 --limit 100` (기존 계약 그대로, 최초 100건만).
  결과에 따라 중간/말미 구간 확대 여부를 별도로 정한다(한 번에 전체로 확대하지 않는다).
- 목적: 방향 정책 전환 이전 코드로 수집된 이 파일이 21f8c124처럼 국소적 문제인지,
  이미 문서화된 8.6% 불일치처럼 전면적 문제인지 확인.
- 예상 I/O: 헤더+100건, 8 MiB 이내(도구 상한). 실측 소요 시간은 근거가 없어 숫자로 추정하지 않는다.
- 허용 시간/예산: 도구 자체 상한(2초 SQLite 진행 예산, 0.5초 잠금 대기)을 그대로 따른다.
- 성공 기준: `sample_consistent`(종료 코드 0)이고 `mismatches`/`quality_issues`가 비어 있으면
  다음 단계(중간/말미 확대)를 검토할 근거가 생긴다. 이것만으로 연구 입력 합격을 의미하지 않는다.
- 즉시 중단 조건: 종료 코드 3(입력/구조/예산 오류), 또는 `quality_issues`/`mismatches`가
  나오면 확대 조회를 멈추고 해당 순번·정책만 조사한다(기존 21f8c124 절차와 동일).

- [ ] **후보 파일 하나 지정:** 경로·session_id·날짜·수집 시간 범위·종목/venue·크기·종료 근거를 기록한다.
  날짜 폴더나 closed 헤더만으로 정상 입력을 확정하지 않는다. 기존 근거를 우선 활용한다.
- [ ] **품질 문제 처리 방향 결정:** 현재 계약을 통과할 다른 닫힌 파일을 사용할지,
  별도 파생 구간 사용 계약이 필요한지 결정한다. 후자는 아직 구현 완료된 기능으로 취급하지 않는다.
- [ ] **파생 구간이 필요할 때만:** 선택/제외 기준, 원본 순번·출처 추적, 오류 보존,
  전략에 필요한 선행 구간과 시가 정보, 결과 해석 범위를 먼저 설계하고 합성 검증한다.
  선택하지 않으면 근거와 함께 해당 없음으로 표시한다. 원본 오류 삭제·방향 추정으로 통과시키지 않는다.
- [ ] **시험 범위 확정:** 종목 하나·청산 규칙 하나·독립 계좌 하나와 비용·수량·지연·호가 나이 등을 기록한다.
  venue가 unknown이면 NXT 재현으로 인증하지 않는다. 실행 가능성과 전략 진입에 충분한 입력인지 구분한다.

## 3. 장외 실행 전 준비

- [ ] **실행 계획 확정:** 대상·장외 시간·허용 I/O/시간·중단 기준·출력 위치·여유 공간을 정한다.
  수집과 겹치지 않게 하고 최신 종료 상태를 대조한다. 이 체크리스트 작성은 실제 실행 지시가 아니다.
- [ ] **실행 경로 선택:** 화면의 32 MiB·100,000 raw 상한 안인지 확인한다.
  초과 파일은 명시적 CLI 계획이 필요하다. 종목 하나를 골라도 전체 입력을 읽으므로 부하가 작다고 가정하지 않는다.
- [ ] **입력 전체 검증:** 승인된 장외 범위에서 전체 순번·체크섬·품질 계약을 검사하고 근거를 보존한다.
  실패하면 연구 입력 합격으로 처리하지 않는다. 기존 300건 표본이나 저장 종료 확인으로 대체하지 않는다.

## 4. 첫 시험 실행과 결과 대조

- [ ] **시험 실행 1회:** 확정된 입력·설정·코드 버전으로 실행하고 입력 출처와 결과 경로를 남긴다.
- [ ] **결과 상태 확인:** 전체 입력 처리 여부·오류·체결 없음·미청산을 구분한다.
  failed 결과의 중간 수익을 정상 성과로 해석하지 않는다.
- [ ] **대표 사례 수동 대조:** 원본 이벤트부터 신호·주문·체결 가격/수량까지 연결해 확인한다.
  진입 사례가 없다면 원인을 기록하고, 체결 경로의 실데이터 검증은 미완료로 남긴다.
- [ ] **재현성 대조:** 같은 입력·설정·코드의 재실행에서 주문·체결·경제적 결과가 같은지 확인한다.
  실행 ID·생성 시각처럼 달라져야 하는 메타데이터는 비교에서 구분한다. 재실행도 장외 I/O 계획에 포함한다.
- [ ] **첫 시험 종료 기록:** 성공/실패 원인, 확인한 범위와 남은 한계를 HANDOFF에 남긴다.
  위 항목의 근거가 있어야 시험 완료로 표시한다. 전략 수익성 확인으로 확대하지 않는다.

## 5. 첫 시험 이후 — 전략 평가

- [ ] 여러 날짜·시장 상황·종목에서 품질이 확인된 입력과 전략 진입 사례를 축적한다.
- [ ] 학습/조정에 쓴 구간과 평가 구간을 분리하고, 비용·지연·체결 가정 변화에 따른 결과를 비교한다.
- [ ] 손익뿐 아니라 거래 수·손실 구간·미체결·미청산·데이터 제외 내역을 함께 검토한다.
  필요한 데이터 기간은 거래 빈도와 평가 목적에 맞춰 정하며 현재 용량만으로 충분성을 선언하지 않는다.

## 별도 진행 — 첫 시험의 필수 선행 조건이 아닌 것

- [x] 작은 합성 raw 압축·복원 시제품과 바이트 검증 준비.
  근거: `97e3777`, `6e1029f`, [보관 테스트](tests/test_raw_archive.py).
- [ ] 압축·복원의 실제 대용량 적용과 용량/속도/내구성 검증. 원본 자동 삭제는 범위 밖이다.
- [ ] 실피드 장시간 부하·장애 복구·OCX/NXT 전환의 운영 실측.
  관련 입력을 연구에 사용할 때 필요한 원천 확인은 2~3단계에서 따로 충족한다.
- [ ] 실거래 주문 연결과 운영 승인. 시험 백테스트 완료와 별개다.

운영 raw·Daily_baseline·old_data·사용자 변경·오류 근거를 보존한다.
상세 계약을 이 문서에 중복 확장하지 않고 담당 문서에 연결하며, 완료된 항목은 근거와 함께 유지한다.
